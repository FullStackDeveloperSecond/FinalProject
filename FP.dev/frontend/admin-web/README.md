# DoSelect Admin Web

DoSelect 懂選的管理後台。Vite 與 Vue Router 以 `/admin/` 作為部署基底；實際授權仍由後端 Policy 執行。

```powershell
npm ci
npm run dev
npm run typecheck
npm run lint -- --max-warnings 0
npm test
npm run build
```
