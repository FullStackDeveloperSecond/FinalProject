# 會員、營運與組裝 Demo 測試補充工作包

更新：2026-09-10。本文件不含密碼；沿用原建置手冊及本機受保護測試工作包。這是本機預覽，不是正式發布／安全驗收證明。

## 最新追加：三種優惠與財務版面（已套用目前隔離 Demo）

本節優先於下方歷史 CREATOR10 驗收敘述。2026-09-10 已備份、套用 schema 及活動，並以原 AI／Email 開關恢復前後台及 API；健康檢查均 Ready。SMTP／OpenAI 真實呼叫不在本次驗證範圍。

- CREATOR10：已停用並保留為 2026 年 8 月活動紀錄；保留既有訂單及用券紀錄。
- SCHOOL2026：2026/9/1～9/30（臺北時間），全商品、無最低消費、無折抵上限、會員／訪客皆可、每人一次。商品總數 1 件九五折，2 件以上九折，同 SKU 數量也計入；不折運費／組裝費。退貨沿用該品項原成交實付金額，不追回九折與九五折差額。
- MEMBER100：全商品滿 NT$1,000 折 NT$100；每位會員一次，依伺服器原始入會時間算 12 個月，滿周年即到期，包含仍未滿一年的既有會員；訪客不可使用。
- 一張訂單只能使用一張優惠券，不合併。更換券後核對即時折扣、應付金額及錯誤提示。訪客一次限用沿用驗證電子郵件的雜湊識別，不宣稱能辨識跨信箱的同一真人。
- 後台發票／退款列表：篩選欄位與按鈕留間距；明細採對齊格線、長編號可換行、表格可橫向捲動。沿用品牌風格，沒有改變退款／作廢權限或自動執行財務操作。
- 升級使用 `database-deploy/coupon-quantity-membership/00-manifest.md`，先備份、再精確 schema 升級、再維護活動；不要重設 Demo 或重新匯入一萬筆。依使用者裁定，其他 29 張 DEMO 展示券也已停用，歷史均保留；目前只有 SCHOOL2026、MEMBER100 啟用。
- 人工驗收：1 件 NT$50,000 應折 NT$2,500；同 SKU 2 件各 NT$50,000 應折 NT$10,000。入會禮於商品金額 999 應拒絕、1,000 應折 100；滿周年／訪客應拒絕。先以報價驗證，不必為看折扣而實際建單或消耗用券資格。
- 目前 Demo 維護命令已成功；更新前後 650 筆訂單、100 筆用券紀錄及 0 筆 OrderCoupon 快照不變，35 筆活動管理 Audit。備份及 VERIFYONLY／雜湊已驗證，真正備份還原仍未測。使用者完成 MFA 後，已實機複核優惠規則／即時狀態篩選、發票搜尋區及退款列表／明細格線；沒有繞過驗證。此庫沒有模擬發票資料，所以發票明細只有元件測試，實機資料驗收未測；未為驗收而開票／退款。另一台 SMTP／OpenAI 仍是未測試且不阻擋無關工作。

## 入口與環境

- 前台：http://localhost:5173
- 後台：http://localhost:5174/admin/
- 本輪使用隔離資料庫 `DoSelectDemo_e2f7abe1602840a4aa6574599f341a11`，未重設既有帳號或訂單。
- 系統 CLI：在 `FP.dev` 執行 `./scripts/status.ps1` 查看狀態。SMTP／OpenAI 開關已啟用，但本輪未測真實寄信／模型呼叫。
- 前端更新後若白畫面，先重新整理。下次依建置手冊重新執行 `start-all.ps1` 時會強制更新 Vite 相依模組 URL，避免瀏覽器保留舊共用模組。不需要清除帳號、購物車或資料庫。

## Demo 帳號快捷填入

1. 開啟前台或後台登入頁，找到「Demo 帳號快捷填寫」。僅本機開發伺服器顯示，正式 production build 不提供此入口。
2. 選擇本機檔案 `.run/demo-login-ad89d094c36546659ad6f3846eeeb620/demo-login-pack.json`。
3. 按「填入」對應帳號，然後自行按「登入」。前台為 `member-0001@example.invalid`，後台為 `demo-admin@example.invalid`。
4. 後台仍須完成原本的 MFA；工作包不包含繞過驗證的能力。
5. 工作包只載入當頁記憶體，重新整理後需再選檔。可按「卸載工作包」清除已載入的帳號集合；已填入的登入欄位需自行清空或重新整理。

密碼保存在該 JSON，不寫入此文件、前端程式或 Git。請勿公開分享或提交 `.run`。其他電腦需在當地已完成設置的隔離 Demo 以系統 CLI 執行 `./scripts/export-demo-login-pack.ps1`；此操作驗證既有 Demo 密碼後匯出，不重設密碼，也不讀取正式帳號密碼。

結帳頁另有「填入 Demo 聯絡與收件資料（覆寫欄位）」：只填合成姓名、手機及地址；不填／驗證 Email、不勾選條款、不自動送出訂單。展示資料不得實際配送。

## 建議複核順序

1. **會員中心**：登入後按五個精簡按鈕，下方切換訂單、組裝清單、收藏、評價、地址；不是整頁導走。按「回會員資料」返回，編輯時停用切換避免遺失輸入。
2. **商品**：完整頁 24 筆；1／2／3／4 欄對齊。最後一頁不足 24 筆可自然留白。點頁碼與尾頁確認資料變更；詳情應先顯示名稱，再顯示圖片／購買雙欄。
3. **SLA**：切換狀態、優先度、承辦範圍、逾時或排序立即更新；案件編號輸入後約 300ms 查詢，頁碼回第一頁。
4. **會員管理**：由後台「會員管理」進入。SuperAdmin 可查詢、查看遮蔽詳情及啟停用；PrivacyAdmin 只讀。停用／恢復需確認與理由。未驗證信箱、匿名化、已關閉帳號不可用此操作啟用；不可查看密碼或刪除會員。
5. **組裝進度**：後台「營運與物流 → 訂單」開啟訂單詳情。必要時先依原流程「開始處理」。在「組裝工作進度」逐台執行「組裝完成，開始測試」，確認後再執行「測試通過，確認可出貨」。失敗則標記原因，修復後「重新開始組裝」。多台訂單必須全部通過，不能略過測試直接出貨。
6. **批次出貨**：回訂單列表重新勾選，以最新版本建立物流單或標記已出貨。付款、組裝、配送與庫存檢查仍有效；版本衝突需重新讀取，不能重複提交舊資料。Preparing／備貨中不一定已有物流單，是否可開單由伺服器檢查。
7. **註冊／結帳文字**：姓名在註冊表單最上方；錯誤提示繁體中文；公司發票欄位為「公司抬頭」。
8. **頁碼補齊**：案件工作台、後台訂單、庫存保留、匯入明細、營運報表、優惠商品挑選與超商階層選單可直接點頁碼／尾頁。篩選後回第一頁；目前頁資料刪減時會回到有效頁。批次出貨換頁或換篩選會清除舊勾選，避免誤送單。報表 CSV／XLSX 匯出仍包含整份篩選結果，不限目前頁。

## 已驗證與尚未完成

- 本輪前台 Vitest 688／688、後台 492／492；會員／訂單 HTTP＋SQL 26／26，批次出貨 SQL 24／24，SLA Application 16／16；上述測試不等於真人跨頁驗收。
- 前後台 production build、型別檢查及本輪選定變更檔 lint 通過；前台有大於 500 kB chunk 提醒，未為此額外重構。
- 瀏覽器已檢視商品列表、尾頁、商品雙欄及登入工作包入口；未在瀏覽器完成後台 MFA、會員停用、完整下單或組裝出貨旅程。
- **頁碼補齊完成（本地程式）**：上述七處已實作，API 保留舊游標相容模式，總筆數依授權及篩選實際計算。追加驗證：後台完整 495／495；前台完整 688／688 後再補超商單頁縮減回歸 8／8（有重疊，不相加）；Application 65／65、Infrastructure SQL 19／19、API＋SQL 29／29，皆無略過。前後台建置、型別檢查及變更檔 lint 通過。此項 API 尚需隨本批重新啟動，不能僅憑前端熱更新視為線上已套用。
- **客服權限 HTTP 測試已解決**：沙箱無法解密 Windows DPAPI 防偽金鑰造成 500；相同程式改用系統 CLI，單項及完整組別 57／57 通過。未修改程式、安全設定或金鑰，不關閉 CSRF。
- **未測試，非無關工作阻擋項**：另一台電腦 SMTP 收信／OpenAI、完整真人彩排。
- 本地提交 `ced4c883` 保存既有修正，`befe9dc2` 補齊剩餘頁碼；沒有 push／CI／merge。本批沒有略過出貨、授權或 MFA 檢查。
- 安全差異掃描 `57bc5450-f695-4bbc-b983-af234088786e` 已封存：222 個來源盤點項目及另 33 個測試／文件／生成檔完成靜態複核，0 finding。**正式 coverage 仍為 partial**：工具封存時保留了兩筆早期待複核 checkpoint；最終提交的完整 coverage 沒有清除它們。未改寫已封存產物、不宣稱正式安全 Gate 通過。Issue 完成複核及發布仍未完成。
- 建置驗證補查目前 `AddBuildOwnedParts` migration 與 nullable `OwnedPartsJson`，避免只有表／索引數相同便誤判完整。Windows PowerShell 5.1 靜態 safety 檢查已先重現缺失再通過；本輪未執行資料庫初始化或套用 SQL，不把靜態檢查當成第二機部署成功。

## 提交後追加複核（本地程式已修正，瀏覽器尚未測試）

- 後台退貨案件：超過 20 筆時可點頁碼與尾頁；切換退貨狀態立即更新，輸入退貨編號約 300ms 更新並回第一頁。等待寄回的期限已到時應顯示「已逾期」，不是「即將逾期」。
- 退貨檢查：商品狀態與回補判定顯示中文；原退款與處置規則不變。
- 營運報表：訂單狀態改中文多選，選取或清除即更新。桌面可按 Ctrl（macOS 為 Command）多選／取消；未選代表全部。分類／品牌文字仍按「套用篩選」。
- AI 客服：同意／撤回失敗會顯示錯誤，撤回失敗保留問題輸入；不會自行視為操作成功。訂單脈絡的狀態以中文顯示。
- 本輪局部測試後台 21／21、前台 5／5，兩端型別檢查及變更檔 lint 通過；不是完整真人或外部服務驗收。沒有重啟 API、重設資料庫、push 或重跑安全掃描。

## 22:25 本機複核更新（覆蓋上方服務與瀏覽器待辦狀態）

- 已以系統 CLI 重啟原隔離 Demo，最新 API 已載入；前台、後台與 API 維持運行。資料庫未重設、未套用 migration；SMTP／OpenAI 維持啟用，但沒有用寄信／模型呼叫當成本輪測試。
- 你已完成後台登入及 MFA。瀏覽器確認：會員管理可顯示 600 筆及 30 頁，停用篩選立即得到空結果；SLA 優先度篩選立即更新；案件工作台第一筆連結可點開；訂單待出貨勾選立即更新；退貨 150 筆可跳第 8 頁，等待寄回篩選剩 30 筆／2 頁，過期顯示「已逾期」。未停用會員、改動訂單或執行退款。
- 前台實測：商品完整頁 24 筆、尾頁 10 筆，分類切換即時更新；加入 2 件時 navbar 顯示 2 件；CREATOR10 適用商品小計 NT$21,200 折 NT$2,000，結帳應付 NT$19,200；門市品牌→縣市→行政區→門市逐級更新，換品牌清除舊選擇；手機 `0000` 即時顯示格式錯誤；公司抬頭及必填標示正確；政策詳細內容以 Demo 彈窗顯示，閱讀不勾選同意。訪客先驗證信箱再建單的按鈕與限制可見，沒有寄信或提交訂單。
- 瀏覽器發現並補修的文字：案件標題 `Logistics` 等已知分類代碼、前後台訂單配送方式，以及退貨 `NotInspected`／舊 Demo `PendingInspection`。沿用原代碼及既有狀態，不改資料；自由文字保留。
- 本輪購物車新增的 2 件合成商品已移除，恢復 0 件。未刪除任何原始資料。
- 本輪先完整驗證前台 **691／691**、後台 **501／501**；追加標籤與測試後只驗證受影響組別：案件工作台 **14／14**、後台訂單 **14／14**、前台訂單 **15／15**、退貨明細 **14／14**。不要把重疊案例相加，亦不把較早全套結果當成最終版本全套重跑。最終兩端型別檢查及本輪變更檔 lint 通過。
- 組裝前端回歸確認指定工作及訂單版本一起送出、只顯示伺服器允許的操作、失敗必填理由及衝突提示；配合前批 HTTP／SQL 測試使用，**仍不是瀏覽器實際完成整台組裝並出貨**。
- **仍未測試、不阻擋無關工作**：完整會員／訪客下單、SMTP 實際收信、OpenAI 真實呼叫、會員啟停用及組裝出貨的完整瀏覽器旅程、第二台全新建置及含空白路徑啟動。正式 Security coverage 仍為 partial，未取得發布安全 Gate 通過結論；沒有 push、merge 或關閉 Issue。

詳細進度、逐項完成依據與自主裁定見 `FP.sheet/FinalProject/05-規劃/04-稽核與報告/2026-09-06-工作進度確認紀錄.md` 第 15–18 節。本輪證據：`.codex-evidence/final-local-20260909/manifest.json`。
