---
文件狀態: 進行中
最後更新: 2026-08-31
目標檢查點: 2026-08-30
---

# 核心交易整合協調

## 判定

`NO-GO／高風險`：依 2026-08-31 的 `dev@4fffcbf3`，08-30 原定「訪客宅配結帳＋即時模擬付款＋原子保留庫存」完整 E2E **未達成**。PR #52／#55 已交付 Checkout 原子核心、SQL 成功／回滾、Outbox／通知、Playwright 與 Coverage 基礎；PR #29 已交付購物車前端，PR #63 已交付正式套券 Endpoint，PR #68 已釘住 Checkout 整數化與 `GrandTotal = PaymentAttempt.Amount`，PR #71 已交付 Demo 付款完成交易。但仍沒有正式 Checkout Controller／前端、最後一件商品並行／完整 replay 與核心交易 Playwright 旅程。

原始碼重驗確認 PR #71 已能把會員或訪客自己的 PaymentAttempt 轉為終態，並同交易更新 `Order.PaidAmount`、PaymentEvent、History、Audit 與通知 Outbox。DEC-P344～P346 另裁定補齊 `cancelled`、禁止 COD 在履約前透過 Demo Endpoint 入帳，並由付款成功 Outbox 可冪等建立模擬發票。這三項、正式 Checkout 與核心交易 E2E 尚未完成，所以本文件仍維持 `NO-GO`。

這是 08-30 檢查點的實際 No-Go 結果，不更改 09-08 功能開發截止日。只有下列 Gate 依序通過，核心交易才可改為可展示。

## 既定最小旅程

沿用既有 Demo 規格，不新增產品決策：

`訪客購物車 → CREATOR10 → 組裝費／宅配運費 → 建立訂單 → 原子保留多 SKU → 即時模擬付款成功／回呼 → 顯示訂單編號與通知排程`

替代路徑「使用已建立 Pending 訂單完成付款」只供現場備援，不得用來宣告 Checkout E2E 完成。

## 目前交付面

| 節點 | 主責 | `dev` 現況 | 目前阻擋／依賴 | 下一個整合輸出 |
|---|---|---|---|---|
| Cart | terry | PR #28 已合併；API 與 SQL 測試存在 | 前端 PR #29 尚未合併 | 可結帳 Cart 快照與阻擋原因契約 |
| Coupon | yinyin | PR #7、#8、#50、#63、#69 已合併；已有計算／Reader、管理 API、正式購物車套券 Endpoint、Checkout 優惠券快照與批次 Catalog picker | A-23 後台頁面 PR #64 尚未進 `dev`；顧客套券 UI 與完整 E2E 未完成 | 修正並合併 PR #64；補顧客套券 UI 與退款快照跨層驗收 |
| Checkout／Order | haru | PR #52 已合併 Transaction Gateway、Order／Item／狀態／組裝／費用／規格快照及 SQL 證據 | 尚無建單 Endpoint、OpenAPI 與前端 | Checkout Controller、OpenAPI、Typed Client |
| Reservation | terry | PR #52 已合併原子保留與缺貨回滾證據 | PR #36 未合併，並行最後一件與逾時釋放仍缺 | 並行競爭、replay 與逾時釋放 SQL 證據 |
| Payment | yinyin | PR #9 七種付款政策、PR #52 初始 PaymentAttempt、PR #68 整數化／金額鏈與 PR #71 Demo 完成 Endpoint、Owner／Guest、Antiforgery、PaymentEvent、Payment／Order 同交易、History／Audit、通知 Outbox、SQL replay 均已合併 | DEC-P344～P346 的 `cancelled`、COD Delivered／PickedUp 收款邊界、付款成功後冪等模擬發票 Outbox，以及付款重試 Endpoint／完整 E2E 尚未交付 | alex 以 PR #71 為基線補三項差異；完成聚焦 Provider-backed 與核心交易 E2E 後再關閉 M-09 Gate |
| Shipping | terry | PR #52 已合併宅配／超取、Provider、包裹限制、運費／免運與訂單快照 SQL 證據 | Shipment Application／API／UI 流程未完成 | 配送查詢／出貨 API 與組裝宅配 SQL 案例 |
| 跨模組契約／Gate | alex | Idempotency、Policy、中央 Audit、SQL CI、Outbox／通知、Checkout Gateway、Playwright 基礎與共用身分 Fixture 已合併；付款完成責任已定 | 尚無核心交易完整旅程；API Client 等待正式 Checkout／Payment Endpoint 實作 | 契約驗證、Payment PR 直接 Review、核心旅程 Fixture 組裝與整合驗收 |

## 依賴順序與 Gate

1. **GATE-TX-01｜契約凍結**：Checkout Application 只依賴用途專用規則介面與單一 `ICheckoutTransactionGateway`；Infrastructure Gateway 使用共用 `DoSelectDbContext` 加入冪等 Executor 已擁有的交易，直接鎖定／讀寫原子建單所需表，但不得自行提交。其他 Application 模組不得互相呼叫 Repository。此契約與實作已由 DEC-BATCH-027／PR #52 合併至 `dev`。
2. **GATE-TX-02｜SQL 原子交易**：建立 Order、OrderItems、Coupon／費用快照與 InventoryReservation 必須同交易成功或全部回滾；兩個請求競爭最後一件商品時只能一筆成功。
3. **GATE-TX-03｜付款一致性**：`Order.GrandTotal`、`PaymentAttempt.Amount` 與成功後 `PaidAmount` 必須一致；同 Idempotency-Key replay 不重複建單、扣庫存或付款。
4. **GATE-TX-04｜宅配限制**：既定訪客宅配路徑只能使用預付；費用、滿額免運與組裝配送限制由可信 Shipping／Coupon 規則計算並保存快照。
5. **GATE-TX-05｜Provider-backed 整合**：使用 CI connection template 建立專屬 SQL 測試庫，實際套用目前完整 Migration 歷程；涵蓋成功、缺貨、競爭、付款失敗／replay 與整體回滾。不得把只對既有測試模型建庫的案例當作完整 Migration chain 證據。
6. **GATE-TX-06｜E2E**：Typed Client 更新後，瀏覽器從 Cart UI 完成既定訪客旅程，並以 API／SQL 斷言 Order、Reservation、Payment 與通知排程結果。

## 08-30 Go／No-Go 結果

| 檢查 | Go 條件 | 目前 |
|---|---|---|
| 合併基線 | 核心鏈所有必要 PR 已進 `dev`，沒有以堆疊分支代替 | ❌ PR #36／#37 尚未進 `dev`，Checkout／Payment／Shipping 仍無完整垂直 PR |
| 核心契約 | Checkout Query／DTO、錯誤碼與交易 owner 已凍結 | 🟡 交易 owner 已凍結；API／錯誤碼與 OpenAPI 尚待實作校驗 |
| SQL 證據 | GATE-TX-02、03、05 全部通過 | 🟡 成功、缺貨回滾、免運名額、整數化與 PaymentAttempt 金額一致已通過；競爭、完整 replay、付款成功／PaidAmount 與完整鏈仍缺 |
| 前端旅程 | Cart／Checkout／Payment UI 使用產生的 Typed Client | ❌ |
| E2E | GATE-TX-06 可在乾淨環境重跑 | ❌ |

08-30 結果固定為 `NO-GO`。在五項全部轉為 `✅` 前，核心交易維持不可展示，不得用一般 CI 綠燈、手動建立 Pending 訂單或單一模組測試代替。
