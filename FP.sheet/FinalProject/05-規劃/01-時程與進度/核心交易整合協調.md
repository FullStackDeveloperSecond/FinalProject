---
文件狀態: 進行中
最後更新: 2026-09-02
目標檢查點: 2026-08-30
---

# 核心交易整合協調

## 判定

`NO-GO／中高風險`：依 2026-09-02 的 `dev@70015781`，正式 Cart、Checkout API、Shipping Options／Store、Payment、Guest Order Access、Order／Invoice 與 SQL 原子核心均已存在；WP-A03 已 rebase 最新基線並完成 C-14、具體 `PaymentMethod[]`、Latest Payment Attempt 續接與本機 Gate，但最新 head 尚未通過遠端 CI／合併進 `dev`。最後一件商品並行、完整 Checkout replay、訪客跨 Guest 驗證的 Cart→Checkout→Payment Browser E2E，以及 COD 物流命令接線仍未完成，所以核心交易仍不可宣告可展示。

原始碼重驗確認付款完成可同交易更新 `Order.PaidAmount`、PaymentEvent、History、Audit 與通知 Outbox，付款成功發票 Outbox／Consumer、取消與 COD Demo 拒絕亦已進 `dev`。WP-A03 依 DEC-P355 消費上游 Latest Payment Attempt、依 DEC-P353 補齊優惠券後 Shipping／COD 重算，並依 DEC-P354 修正組裝免運的 SQL 欄位長度；rebase 後 customer-web 334 項、Domain 482 項、Application 577 項、相關 SQL Provider／Migration 33 項、Shipping／Payment API focused 19 項，以及 OpenAPI／Typed Client、typecheck、lint、production build 均通過。Review 另補齊組裝購物車只能使用組裝宅配的顯示層回歸，與 Checkout 最終檢核一致。這些證據只證明本機垂直切片，不取代 WP-A04 的跨交易 Browser／SQL 斷言。依 DEC-P347，COD 正式付款寫入仍等待物流狀態命令接線。

這是 08-30 檢查點的實際 No-Go 結果，不更改 09-08 功能開發截止日。只有下列 Gate 依序通過，核心交易才可改為可展示。

## 既定最小旅程

沿用既有 Demo 規格，不新增產品決策：

`訪客購物車 → CREATOR10 → 組裝費／宅配運費 → 建立訂單 → 原子保留多 SKU → 顯示訂單編號 → Email 限單驗證 → 即時模擬付款成功／回呼 → 顯示訂單與通知排程`

替代路徑「使用已建立 Pending 訂單完成付款」只供現場備援，不得用來宣告 Checkout E2E 完成。

## 目前交付面

| 節點 | 主責 | `dev` 現況 | 目前阻擋／依賴 | 下一個整合輸出 |
|---|---|---|---|---|
| Cart | terry | API、SQL、前端、組裝整組移除、結帳前重驗與 C-13 配送預覽均已合併 | 完整 Cart→Checkout Browser 旅程仍缺 | WP-A04 重用正式 Cart 與 RowVersion |
| Coupon | yinyin／alex | `dev` 已有計算／生命週期、管理 API、A-23、Checkout 優惠券快照與用途 Catalog picker；WP-A03 本機接上 Checkout 套用與 Shipping Quote | Cart 頁套券 UI、持久化 Cart Coupon Endpoint、完整 E2E 未完成 | WP-A03 Git Gate；Cart Coupon 仍由原 owner 另包交付 |
| Checkout／Order | haru／alex | Transaction Gateway、正式 `POST /orders`、政策版本、Order／Item／快照、OpenAPI 已進 `dev`；WP-A03 本機完成 C-14 | WP-A03 尚待 Git Gate；完整 replay、最後庫存競爭與 WP-A04 E2E 未完成 | 先合併 WP-A03，再建立隔離核心交易旅程 |
| Reservation | terry | PR #52 已合併原子保留與缺貨回滾證據；PR #36 已合併庫存保留／異動／盤點後台 API | 堆疊前端 PR #37、並行最後一件與逾時釋放仍缺 | 並行競爭、replay 與逾時釋放 SQL 證據 |
| Payment | yinyin／alex | 七種付款政策、初始／重試 Attempt、Demo complete、付款頁、整數金額鏈、發票 Outbox／Consumer 與 Owner-scoped Latest Attempt 均已進 `dev`；WP-A03 本機已接上續接 UI | WP-A03 尚待遠端 CI／合併；COD 正式物流命令接線與完整 E2E 尚未交付 | WP-A03 合併後由 WP-A04 驗證預付續接；物流 writer 依 DEC-P347 接線 |
| Shipping | terry／alex | 宅配／超取、Provider、包裹限制、運費／免運、配送選項／示範門市 API、Provider-backed 與 C-14 共用元件已進 `dev`；WP-A03 本機補 Coupon Quote 後運費／COD | WP-A03 尚待 Git Gate；批次出貨、物流狀態命令與 COD 接線未完成 | WP-A03 依 DEC-P351／P353 消費具體付款陣列與可信優惠試算；物流 owner 補狀態命令 |
| 跨模組契約／Gate | alex | Idempotency、Policy、中央 Audit、SQL CI、Outbox／通知、OpenAPI Typed Client、Guest E2E 與 WP-A03 本機驗證均存在 | WP-A03 尚未進 `dev`；WP-A04 核心旅程未開始 | WP-A03 Git Gate，接著組裝隔離 SQL／Browser 核心旅程 |

## 依賴順序與 Gate

1. **GATE-TX-01｜契約凍結**：Checkout Application 只依賴用途專用規則介面與單一 `ICheckoutTransactionGateway`；Infrastructure Gateway 使用共用 `DoSelectDbContext` 加入冪等 Executor 已擁有的交易，直接鎖定／讀寫原子建單所需表，但不得自行提交。其他 Application 模組不得互相呼叫 Repository。此契約與實作已由 DEC-BATCH-027／PR #52 合併至 `dev`。
2. **GATE-TX-02｜SQL 原子交易**：建立 Order、OrderItems、Coupon／費用快照與 InventoryReservation 必須同交易成功或全部回滾；兩個請求競爭最後一件商品時只能一筆成功。
3. **GATE-TX-03｜付款一致性**：`Order.GrandTotal`、`PaymentAttempt.Amount` 與成功後 `PaidAmount` 必須一致；同 Idempotency-Key replay 不重複建單、扣庫存或付款。
4. **GATE-TX-04｜宅配限制**：既定訪客宅配路徑只能使用預付；費用、滿額免運與組裝配送限制由可信 Shipping／Coupon 規則計算並保存快照。
5. **GATE-TX-05｜Provider-backed 整合**：使用 CI connection template 建立專屬 SQL 測試庫，實際套用目前完整 Migration 歷程；涵蓋成功、缺貨、競爭、付款失敗／replay 與整體回滾。不得把只對既有測試模型建庫的案例當作完整 Migration chain 證據。
6. **GATE-TX-06｜E2E**：Typed Client 更新後，瀏覽器從 Cart UI 完成既定訪客旅程，並以 API／SQL 斷言 Order、Reservation、Payment 與通知排程結果。

## 08-30 Go／No-Go 結果

| 檢查 | Go 條件 | 目前 |
|---|---|---|
| 合併基線 | 核心鏈所有必要 PR 已進 `dev`，沒有以堆疊分支代替 | ❌ PR #36 已進 `dev`，但堆疊前端 PR #37 尚未進 `dev`；Checkout／Payment／Shipping 仍無完整垂直 PR |
| 核心契約 | Checkout Query／DTO、錯誤碼與交易 owner 已凍結 | ✅ API、政策、配送／門市、具體付款方式、Guest Scope 與 Typed Client 已校驗；DEC-P351 禁止 `prepaid` 與前端硬寫 |
| SQL 證據 | GATE-TX-02、03、05 全部通過 | 🟡 成功、缺貨回滾、免運名額、整數化、PaymentAttempt 金額一致、付款成功與 PaidAmount 已通過；競爭、完整 Checkout replay 與完整鏈仍缺 |
| 前端旅程 | Cart／Checkout／Payment UI 使用產生的 Typed Client | 🟡 WP-A03 rebase 後通過 334 項測試、Latest Attempt 續接與優惠券後 Shipping／COD 重算，尚待遠端 CI／合併 |
| E2E | GATE-TX-06 可在乾淨環境重跑 | ❌ |

08-30 結果固定為 `NO-GO`。在五項全部轉為 `✅` 前，核心交易維持不可展示，不得用一般 CI 綠燈、手動建立 Pending 訂單或單一模組測試代替。
