# DATA-RC-01｜固定展示 Seed 執行紀錄

- 日期：2026-09-07（Asia/Taipei）
- 基準：`dev@acb27f6142db533be980321b06befa2f36eec9c8` 的工作分支
- FeatureProfile：`implemented-features-v1`
- 固定亂數種子：`20260907`
- 錨定期間：`2026-03-01T00:00:00Z`～`2026-08-31T23:59:59Z`
- 資料庫：本機 allowlist `DoSelectDemo`；沒有 Production 連線或資料

## 最低成本與安全邊界

依 [[05-規劃/03-需求與決策治理/決策/02-已寫回/DEC-BATCH-071-固定展示Seed產生器邊界定版]]，重用既有 EF Core Entity／Migration 與明確手動命令，不新增 Schema、服務或套件。命令只接受 `DoSelectDemo`／`DoSelectDemo_<32 hex>`，部分非空資料拒絕，不清庫、不覆寫；合成帳號不可登入，沒有真實個資、Secret 或外部圖片。

## 實際執行

命令：

```powershell
./scripts/seed-demo-data.ps1 -DatabaseName DoSelectDemo
```

第一次結果：

```json
{"FeatureProfile":"implemented-features-v1","RandomSeed":20260907,"PeriodStartUtc":"2026-03-01T00:00:00Z","PeriodEndUtc":"2026-08-31T23:59:59Z","MainBusinessRecordTotal":10000,"Created":true,"Counts":{"members":600,"addresses":500,"products":250,"skus":750,"productSpecificationValues":1600,"orders":650,"orderItems":1600,"paymentAttempts":700,"shipments":550,"inventoryMovements":800,"supportTickets":250,"supportMessages":900,"returnRequests":150,"refunds":120,"productReviews":250,"favorites":200,"couponsAndRedemptions":130,"aiSearchFunnelEvents":0}}
```

第二次結果：

```json
{"FeatureProfile":"implemented-features-v1","RandomSeed":20260907,"PeriodStartUtc":"2026-03-01T00:00:00Z","PeriodEndUtc":"2026-08-31T23:59:59Z","MainBusinessRecordTotal":10000,"Created":false,"Counts":{"members":600,"addresses":500,"products":250,"skus":750,"productSpecificationValues":1600,"orders":650,"orderItems":1600,"paymentAttempts":700,"shipments":550,"inventoryMovements":800,"supportTickets":250,"supportMessages":900,"returnRequests":150,"refunds":120,"productReviews":250,"favorites":200,"couponsAndRedemptions":130,"aiSearchFunnelEvents":0}}
```

第二次執行為 no-op；總數、分類計數與固定鍵均未改變。執行時只有 repository 既有 `ReturnRequest.Priority` EF database-default sentinel warning；它不改變 Seed 結果，後續不得把警告誤報為零。

## Provider-backed 驗證

`DemoDataSeedSqlServerTests` 使用隨機命名 `DoSelectDemo_<32 hex>` 可拋棄資料庫，驗證：

- 由不存在／空資料庫自動套用現行 Migration 並建立精確 10,000 筆。
- 第二次執行 `Created=false`，首筆 Product／Order 固定 PublicId 不變。
- 遠端 SQL Server 來源在連線前拒絕；部分非空資料拒絕且既有筆數不變。
- SQL Server `DBCC CHECKCONSTRAINTS WITH ALL_CONSTRAINTS` 無違反。
- completed／cancelled Order、PaymentAttempt timestamps、Return／Refund、低庫存與期間不變量通過。
- 測試結束以精確隨機資料庫名稱 `EnsureDeleted` 清理；不碰 `DoSelectDemo` 或其他資料庫。

系統環境 focused 結果：4／4 Pass，約 15 秒。沙箱第一次因 Windows SQL TLS／SSPI 環境限制失敗，沒有產品資料異動；依 alex 指示改用已登入／已設定的系統環境後通過。安全預審收窄連線／Migration 邊界後，既有 `DoSelectDemo` 再次回傳 `Created=false` 與完全相同計數。

完整 `DoSelect.Infrastructure.Tests` 於系統環境 1,231／1,231 Pass。Codex Security 對程式碼凍結後的固定 source snapshot 完成 5／5 檔案審查：scan `e0e84208-c54c-4b2b-86ca-5e8319d8c3da`，snapshot digest `3c3cf15b2728ba5134c7cd52d4d6d3b73ecf8f76c8a1d28feee20f8cb637d055`，0 candidate／0 finding／0 deferred、coverage `complete`。先前 snapshot 均因後續仍有安全或空白資料庫邊界修正而失效／取消，不作最終通過證據；TAC `not_granted` 為工具明定的 advisory warning，未降低或略過掃描。

## 結論與剩餘 Gate

DATA-RC-01 已由 PR #144 通過 exact-head review、Security 與 Required CI Run `34071018985`，於 2026-09-07 squash merge 至 `dev@a50bb8e0aafb0794181f18143c273da5c40777b2`。DATA-RC-02 特殊分布機器摘要、DATA-RC-03 故意失敗 validator 與 DATA-RC-04 報表 P95 不在本項中提前宣告完成。
