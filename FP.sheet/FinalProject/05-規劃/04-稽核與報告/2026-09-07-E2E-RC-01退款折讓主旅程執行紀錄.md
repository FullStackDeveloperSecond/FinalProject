# 2026-09-07 E2E-RC-01 退款／折讓主旅程執行紀錄

## 結論

E2E-RC-01 在 `dev@e5d46f4a53a5199b6dd1a0afabcf592baa05a593` 完成重驗：兩條 H-R03 Playwright 旅程 2／2 通過，退款與折讓 SQL Server provider-backed 測試 48／48 通過、0 skipped。既有正式 UI、API 與資料庫路徑已共同證明退貨核准、退款審核／執行、Order／Return／折讓投影、金額、冪等、Audit 與跨 Actor 拒絕零副作用；本輪不需要新增產品碼、測試專用 API、Schema、Migration 或依賴。

## Run contract

| 欄位 | 值 |
|---|---|
| Run ID | `e2e-rc-01-20260907-dev-e5d46f4a` |
| Revision | `e5d46f4a53a5199b6dd1a0afabcf592baa05a593`；執行前 tracked working tree clean |
| Calling Skill | `verify-vue-dotnet-e2e`；T2 evidence 由 `capture-test-evidence` 保存 |
| Browser | Playwright `1.62.1`、`admin-chromium`、1 worker、CI 設定 1 retry；實際 0 retry |
| Provider | .NET SDK `10.0.400`、本機 SQL Server、測試專屬資料庫 |
| Fixture | repository minimal E2E seed；背景工作與模擬付款只在 E2E 啟用 |
| 門檻 | 選定 Browser 與 SQL 測試全部通過，且 0 skipped |
| 資料邊界 | 合成測試資料；無 Production data、真實個資、Secret 或外部付費呼叫 |

## E2E result

- Verdict: Pass
- Journeys: H-R03 全額退款＋折讓；H-R03 部分退款＋Actor Scope
- Environment: 隔離、隨機 allowlist `DoSelectE2E_<32 hex>`，測試後刪除
- Product code changed: No

## Coverage

| Journey | Actor | Expected outcome | Result | Evidence |
|---|---|---|---|---|
| 送達訂單→退貨申請→免寄回核准→退款核准／執行→自動發票折讓 | 顧客＋財務管理員 | Return `AwaitingRefund→Completed`；Refund `PendingReview→Approved→Succeeded`；Order `Refunded` 且金額相等；核准、執行與折讓同鍵重播不重複；前後台投影一致 | PASS | Playwright `admin.spec.ts:789`；HTML report |
| 兩件退一件＋越權負例 | 合法訪客、另一訪客、匿名者、財務管理員 | 退款後 Order `PartiallyRefunded`；另一訪客讀取／新增回 404，匿名核准回 401；前後快照與全退貨集合不變 | PASS | Playwright `admin.spec.ts:1108`；HTML report |
| 退款核准／執行資料不變量 | SQL Server provider | Refund、Return、Order、分攤、Audit、Idempotency 同交易；成功唯一，衝突／撤權／失敗零寫入 | PASS | `RefundExecutorSqlServerTests` 38／38 |
| 折讓資料不變量 | SQL Server provider | 折讓明細、Invoice 狀態、Audit、Idempotency 原子落地；同鍵重播唯一；跨發票、RowVersion、Audit 失敗全回滾 | PASS | `InvoiceAllowanceWriterSqlServerTests` 10／10 |

## Execution

| Command | Discovered | Executed | Passed | Failed | Skipped |
|---|---:|---:|---:|---:|---:|
| `test-customer-e2e.ps1 -Project admin-chromium -JourneyTitle <two H-R03 titles>` | 2 | 2 | 2 | 0 | 0 |
| `dotnet test DoSelect.Infrastructure.Tests.csproj --filter <refund-or-allowance>` | 48 | 48 | 48 | 0 | 0 |
| 合計 | 50 | 50 | 50 | 0 | 0 |

Browser 測試本體 30.9 秒；SQL 測試本體 25 秒。第一次從 sandbox 啟動時，在任何資料庫建立前即因 Windows 整合驗證無法建立 SSPI context 而停止；未改測試、設定或範圍，改以 alex 指定的系統環境身分重跑後通過，因此分類為環境前置失敗，不計為產品 retry 或 flaky。

## Artifacts

| Artifact | 保存 | Bytes | SHA-256 |
|---|---|---:|---|
| Playwright HTML report | 本機忽略目錄 `.run/e2e-rc-01-20260907/playwright-report/index.html` | 546,119 | `2f466f6e1e97fd53463fdd2e586da58c214c4eab61a37fe08107f4b42efa6ebb` |
| SQL TRX | 本機忽略目錄 `.run/e2e-rc-01-20260907/sql/e2e-rc-01-sql.trx` | 74,841 | `146b6cc8607f28bf579a7c6d7bfc3a240dde33e404eb5c4d6f1513c4527a0614` |
| 去識別 T2 manifest | [[2026-09-07-E2E-RC-01測試證據清單.json]] | 版本控制 | 由合併後 Git blob 保存 |

兩份 runner artifact 已掃描測試帳密、TOTP seed、連線字串、Authorization 與 Cookie 樣式，結果 0 match。通過時依 repository 設定不產生 trace、截圖或影片；HTML report 是本輪必要的 Browser 報告。失敗時才保留 trace／screenshot／video，避免無目的蒐集瀏覽器狀態。

## 範圍裁定與限制

1. E2E-RC-01 的完成定義是「最小必要主旅程」而不是窮舉退款的所有排列。PR #117 已把主旅程納入正式 UI／API；本輪 current-dev 重跑加上 SQL assertions 已補齊證據鏈，因此不新增 raw SQL test hook 或測試專用 Endpoint。
2. 零淨額退款仍未做 Browser 變體；本輪 `RefundExecutorSqlServerTests` 已覆蓋零淨額核准取消、回滾與冪等，另有既有 `ReturnRefundCreationTests` 保護建立階段。它保留為非主旅程限制，不阻擋 E2E-RC-01／DES-21 結案，也不代表 DES-22 的 E2E-RC-02 已完成。
3. Browser 旅程本身會經 UI→API→SQL Server；額外 SQL assertions 由相同 production use case 的 provider-backed 測試驗證，不建立可被產品呼叫的資料庫診斷面。
4. 本輪建立的隨機 E2E 資料庫與退款固定測試庫均已清理。另查得一顆建立於 2026-08-31 的既有折讓測試庫；它早於本輪且不屬本次建立，依資料安全原則未刪除、未修改。

## 歷史與追溯

- PR #117 head `b8c35f285be70bc5210d5b1bc100fe6fc9601bee` 曾由 `NaughtyJade` 正式核准，CI Run `34017550035` 全綠，於 2026-09-06 squash merge 為 `ba4848957732cec90455456f15bb04b31d253bf6`。
- 本輪是 `dev@e5d46f4a...` 的 fresh evidence，不以舊 PR 綠燈代替現在的測試結果。
- E2E-RC-02／DES-22 的發票作廢或部分折讓非主路徑仍獨立保持待辦。
