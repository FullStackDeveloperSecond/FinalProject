---
文件狀態: 已完成
最後更新: 2026-09-08
追蹤項目:
  - ENV-RC-05
  - QA-08
  - DEV-07
---

# ENV-RC-05 授權矩陣執行紀錄

## 結論

- 本地候選 revision：`8b56d0d820da14e3416cb39c5538ce5a966f72c0`。
- 會員訂單、組裝清單、後台優惠券與退貨能力的 Actor A／B、錯誤角色、一致 404／403 與拒絕後零副作用已補齊。
- exact-head focused 證據合計 `182／182` 通過、0 失敗、0 略過。
- 功能級矩陣缺口已關閉；PR #163 head `91fc6cedc35aeb041ce25bd3b45333e882f51224` 的 Secret／Package／Security review 與 exact-head `CI Required` 全綠，並於 2026-09-08 squash merge 為 `dev@7134a3a9654c6ecf89ac6473db52e7883720f0cd`，`ENV-RC-05`／`QA-08`／`DEV-07` 結案。

## 最低成本範圍裁定

沿用既有 SQL Server fixture、WebApplicationFactory、真實 Cookie／Antiforgery 與私有檔案儲存，只增加拒絕路徑的測試與前後快照。不修改正式 API、Policy、Schema、Migration 或套件。純文件或人工勾選無法證明 SQL 查詢範圍與拒絕後副作用，因此不採用。

## 覆蓋結果

| 範圍 | 證據 | 拒絕後斷言 | 結果 |
|---|---|---|---|
| 會員訂單列表／明細／取消 | SQL Provider `EfOrderServiceTests` | B 列表不含 A；B 明細與取消回 `resource_not_found`；Order／RowVersion、庫存保留、優惠券、歷程、Movement／Audit 不變 | PASS |
| 組裝清單 Update／Delete／Share／Revoke／AddToCart | SQL Provider `EfBuildListServiceTests` | B 皆回 `resource_not_found`；Build／Item／Share Token／Cart／CartItem／Idempotency 前後相同 | PASS |
| 後台優惠券 Create／Update／Action | SQL Provider `AdminCouponServiceSqlServerTests` | 已撤銷角色的管理員三種寫入皆 403；Coupon／Scope／RowVersion／Audit 不變 | PASS |
| 會員／訪客退貨建立、明細與附件 | 真實 HTTP／SQL／磁碟 `ReturnsMemberHttpTests`；Application `ReturnServiceTests` | 會員 B 與 Guest B 皆 404；Return／RowVersion／Attachment metadata 不變；錯誤 actor 不呼叫檔案儲存 | PASS |
| 後台退貨全路由 | 真實 HTTP／SQL `ReturnsAuthorizationHttpTests` | CatalogManager 對 List／Detail／Shipment 讀取及 Review／Receive／Inspect／Extend／Shipment／Event 寫入皆 403；退貨、歷程、檢查、附件、物流、退款、庫存與 Audit 筆數不變 | PASS |

## exact-head 測試證據

| Suite | 通過／總數 | TRX SHA-256 |
|---|---:|---|
| Infrastructure auth matrix | 97／97 | `27BF5809137B1F75FDE983A0FB519EA4B05F99CBCD1A5A421A3BC02463EC4A84` |
| Returns HTTP／SQL／Disk | 43／43 | `04CB38F4965F7C14C2B90EE400FB9CFC2095194B3EDE7993E906D4DD132D2341` |
| Returns Application | 42／42 | `9F49D5ECED9A063E04499E78E1A86A69915ED2B8E62C2001397B093AD03CB9DE` |

TRX 保留在 gitignored `.run/evidence/env-rc-05/exact-head-8b56d0d8`，不提交機器身分、連線字串、Cookie、Token 或業務資料列。機器可讀清單見 [[2026-09-08-ENV-RC-05測試證據清單.json]]。

## 執行邊界與待驗項

- 受限沙箱的第一次 SQL 執行在 fixture 建庫前因本機加密／認證環境失敗，沒有任何產品 assertion 執行；依 alex 既有授權改用系統 PowerShell 重跑後通過。
- 完成後唯讀盤點看到 2 個 2026-08-27／28 建立的舊 Returns 測試庫；它們早於本輪且不是本輪產物，保留不動。本輪 fixture 建立的資料庫均完成 teardown。
- API／Application 專案在 `--no-restore` 執行時出現 `NU1900`（NuGet 弱點資料服務索引當下不可取得）。本輪不將其改列為非阻擋；Package／Vulnerability Gate 仍必須由 exact-head Required CI 成功證明。
- 這些授權、資料隔離與安全檢查均可在現有環境執行，故不適用「未測試／非阻擋」例外。

## 遠端交付與最終覆核

- PR：[#163](https://github.com/FullStackDeveloperSecond/FinalProject/pull/163)，head `91fc6cedc35aeb041ce25bd3b45333e882f51224`。
- Codex Security：`0f0bdf12-07b8-4ee4-b93f-80403b5dbecf`，8／8 變更檔案覆蓋完成，0 finding／0 deferred。
- Required CI：Run `34173145044`；Secret Scan、Package Source Evidence、AI Evaluation Contract、Backend、雙 Frontend、Browser E2E 與 `CI Required` 全部成功。
- 最終 exact-head review：0 actionable finding、0 comment、無 head 漂移。
- GitHub 不允許同帳號核准自己建立的 PR；在所有硬 Gate 通過後依既有授權使用 admin bypass，僅採 squash merge。PR 於 `2026-09-08T00:41:10Z` 合併，merge commit `7134a3a9654c6ecf89ac6473db52e7883720f0cd`。
