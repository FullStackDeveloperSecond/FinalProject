# 2026-09-07 E2E-RC-02 發票作廢拒絕旅程執行紀錄

## 結論

E2E-RC-02 在 commit `569eb6a07428c8cc164e3a0af1b55bb4a3137fb6` 完成本機 T2 重驗：一條正式後台 UI→API→SQL Server 高風險非主路徑 1／1 通過，`InvoiceAllowanceWriterSqlServerTests` 10／10 通過、0 skipped。成功退款後，管理員在發票頁嘗試作廢會收到 `invoice_allowance_required`；發票仍為 `issued`、RowVersion 未變、折讓集合為 0，provider test 另證明 Audit 為 0。PR #152 再通過完整 Security、Required CI 與 exact-head review，於 `2026-09-07T09:59:52Z` squash merge 為 `dev@4132fe43`；E2E-RC-02／DES-22 已完成。

## Run contract

| 欄位 | 值 |
|---|---|
| Run ID | `e2e-rc-02-20260907-commit-569eb6a0` |
| Revision | `569eb6a07428c8cc164e3a0af1b55bb4a3137fb6`；兩組正式 run 前後 tracked working tree clean |
| Calling Skill | `verify-vue-dotnet-e2e`；T2 evidence 由 `capture-test-evidence` 保存 |
| Browser | Playwright `1.62.1`、`admin-chromium`、1 worker、CI 設定 1 retry；實際 0 retry |
| Provider | .NET SDK `10.0.303`、本機 SQL Server、每次測試專屬資料庫 |
| 門檻 | 選定 Browser 與 SQL 類別全部通過，且 0 skipped；本輪建立資料庫全部清理 |
| 資料邊界 | 合成測試資料；無 Production data、真實個資、Secret 或外部付費呼叫 |

## 最低成本範圍裁定

1. E2E-RC-02 要求「發票作廢或部分折讓」至少一條正式展示／高風險非主路徑。既有 H-R03 在退款成功與折讓建立之間已形成正確前置狀態，因此直接重用，不新增另一套 seed fixture。
2. 管理員發票明細頁已有正式作廢表單；API、`Invoice.Manage`、RowVersion、Policy、Audit 與 SQL provider tests 亦已存在。只補 Browser assertion 是第一個完整且最低成本的方案。
3. 本輪不新增產品 API、DTO、Schema、Migration、套件、固定帳密或測試專用資料庫端點。
4. 這條 Gate 選擇「成功退款後禁止作廢」的資料一致性風險；不宣稱成功作廢、零淨額或所有發票／折讓變體均已逐條 E2E。

## Journey 與 assertions

| 步驟 | 正式邊界 | 預期／結果 |
|---|---|---|
| 既有 H-R03 完成退款，尚未建立折讓 | 顧客／管理員 UI、正式退款 API、SQL Server | Refund succeeded；自動發票仍為 `issued`，折讓 0 |
| 管理員開啟發票頁、選擇原因、填寫 note、勾選確認並按「確認作廢」 | Admin Vue UI→Typed API→`Invoice.Manage`→`AdminInvoiceWriter`→SQL Server | PASS：UI 顯示「訂單已有成功退款，必須建立折讓而不能作廢」 |
| 拒絕後由顧客發票查詢重新讀取投影 | 正式 customer API→SQL Server | PASS：status `issued`、RowVersion 與拒絕前相同、allowances 0 |
| 既有折讓命令與重播繼續執行 | 正式 admin API→SQL Server→前後台投影 | PASS：折讓成功且同鍵重播不重複 |
| Provider-backed 全類別 | SQL Server | PASS：10／10；成功作廢、退款後拒絕且 Audit 0、RowVersion、交易、冪等與回滾 |

## Execution

| Command | Discovered | Executed | Passed | Failed | Skipped |
|---|---:|---:|---:|---:|---:|
| `test-customer-e2e.ps1 -Project admin-chromium -JourneyTitle <E2E-RC-02 title>` | 1 | 1 | 1 | 0 | 0 |
| `dotnet test DoSelect.Infrastructure.Tests.csproj --filter FullyQualifiedName~InvoiceAllowanceWriterSqlServerTests` | 10 | 10 | 10 | 0 | 0 |
| 合計 | 11 | 11 | 11 | 0 | 0 |

Browser 測試本體 18.8 秒，SQL 測試本體 23 秒。兩次正式 run 均使用系統環境；Browser runner 使用隨機 allowlist `DoSelectE2E_<32 hex>`，provider 每案使用隨機 `DoSelectAllowanceWriter_<32 hex>`，本輪完成後以建立時間篩選查得殘留數 0。

## Artifacts 與資訊邊界

| Artifact | 保存 | Bytes | SHA-256 |
|---|---|---:|---|
| Playwright HTML report | 本機忽略目錄 `.run/e2e-rc-02-20260907/commit-569eb6a0/playwright-report/index.html` | 532,335 | `317bd1823faff993d65454c25293730e2a869020d8fafee19b7bde10f0c77f2a` |
| SQL TRX | 本機忽略目錄 `.run/e2e-rc-02-20260907/commit-569eb6a0/sql/e2e-rc-02-sql.trx` | 17,170 | `6112ae74eacbd3954a25737748d1d8f9579de7c95b85bf7bc68aefdeecaf3525` |
| 去識別 T2 manifest | [[2026-09-07-E2E-RC-02測試證據清單.json]] | 版本控制 | 由 Git blob 保存 |

兩份 runner artifact 的 Authorization、Cookie、Private Key、GitHub token、JWT 與連線字串值均為 0 match。HTML 有 3 個 `password=` 字樣，逐一分類皆為報告內測試原始碼的未加引號變數指派，0 quoted literal、0 credential value；不把這三筆識別字誤報為祕密，也不把它們隱藏成「敏感樣式 0」。通過時依 repository failure-only policy 不產生 trace、截圖或影片。

## Delivery gate

- Local implementation review: 0 actionable finding。
- TypeScript `vue-tsc -b`: PASS。
- Single-file ESLint: PASS。
- Browser／SQL T2: PASS。
- Codex Security `4a63c87e-bbe7-49d2-9357-6941928aac67`: coverage complete、7／7、0 finding／deferred。
- PR #152 Required CI Run `34107915949`: PASS；Backend 11m54s、Browser 4m51s、雙 Frontend 1m13s。
- Exact-head review: `23edca0827821f8f633811874aa9371ed619da3f`、0 comment／inline comment／review finding。
- 正式 APPROVE 因 GitHub 禁止同帳號自我核准而被拒絕；所有 Gate 通過後依 alex 既有授權管理員 squash merge，`dev@4132fe43`。
- E2E-RC-02／DES-22 已勾選完成；正式未完成數降為 7，專案仍為 `NOT READY`。
