# 2026-09-07 E2E-RC-04 相容性結果邊界執行紀錄

## 結論

E2E-RC-04 在 commit `779d1402dd89fb342e5fb3d8b67ab8a522e5abd7` 完成本機 T2 驗證：一條顧客 UI→API→SQL Server 旅程 1／1 通過，`EfBuildListServiceTests` 與 `CompatibilityCheckServiceTests` 合計 55／55 通過、0 skipped。相同八類組裝以記憶體數量 4 產生 `warning` 且可整套加入購物車，以數量 5 產生 `blocked` 且停用加車；改用刻意缺少已覆核核心規格的合成顯示卡則回 `insufficientData`、顯示需人工確認並停用整套加車。測試與證據已完成，交付仍待 Security、push、PR Required CI、exact-head review 與 squash merge，因此本紀錄不提前關閉 E2E-RC-04／M-17 Browser Gate。

## Run contract

| 欄位 | 值 |
|---|---|
| Run ID | `e2e-rc-04-20260907-commit-779d1402` |
| Revision | `779d1402dd89fb342e5fb3d8b67ab8a522e5abd7`；正式 run 前後 tracked working tree clean |
| Calling Skill | `verify-vue-dotnet-e2e`；T2 evidence 由 `capture-test-evidence` 保存 |
| Browser | Playwright `1.62.1`、`customer-chromium`、1 worker、CI 設定 1 retry；實際 0 retry |
| Provider | .NET SDK `10.0.400`、本機 SQL Server、測試專屬資料庫 |
| 門檻 | Browser 與兩個 SQL 類別全部通過且 0 skipped；相關隔離資料庫全部清理 |
| 資料邊界 | deterministic synthetic catalog／帳號；無 Production data、真實個資、Secret 或外部付費呼叫 |

## 最低成本範圍裁定

1. 相容性引擎、正式前端與 SQL provider tests 已存在；文件或人工操作不足以形成可重跑的三類跨層證據，因此第一個充分方案是補一條 Playwright 旅程。
2. 警告與阻擋直接重用既有相容 SKU：四條單模組記憶體用滿四個插槽時警告，五條時超過插槽數而阻擋；不新增相容性規則或設定。
3. 既有 minimal seed 沒有屬於八類目錄且缺核心規格的可售 SKU，無法真實演示「缺證據」。只新增一個明確標示「規格待覆核」的虛構 GPU，刻意不寫受覆核規格；不新增 Schema、Migration、API、外部來源或正式資料。
4. 清單具備八類元件但缺規格證據時，原購物車說明誤稱「缺少必要元件」。以既有 `BUILD_REQUIRED_COMPONENT` finding 區分缺元件與缺規格，保留兩者 fail closed，不更改後端授權或交易規則。

## Journey 與 assertions

| 類型 | UI→API→Provider 輸入 | 預期／結果 |
|---|---|---|
| Warning | 完整八類相容組裝；記憶體數量 4 | PASS：`warning`、`MEMORY_SLOTS`／`compatibility.memory_slots_low`；「加入購物車」可用且成功 |
| Blocked | 完整八類相容組裝；記憶體數量 5 | PASS：`blocked`、`MEMORY_SLOTS`／`compatibility.memory_slots_exceeded`；顯示 5 個插槽需求超過 4，按鈕停用 |
| Missing evidence | 完整八類；GPU 改為「規格待覆核」合成 SKU | PASS：`insufficientData`／`compatibility.required_data_missing`；顯示缺規格需人工確認，按鈕停用 |
| Provider regression | 組裝清單與相容性兩個 SQL 類別 | PASS：55／55、0 skipped；涵蓋其他規則、保存、加車及 fail-closed 行為 |

## Execution

| Command | Discovered | Executed | Passed | Failed | Skipped |
|---|---:|---:|---:|---:|---:|
| `test-customer-e2e.ps1 -Project customer-chromium -JourneyTitle <E2E-RC-04 title>` | 1 | 1 | 1 | 0 | 0 |
| `dotnet test DoSelect.Infrastructure.Tests.csproj --filter FullyQualifiedName~EfBuildListServiceTests\|FullyQualifiedName~CompatibilityCheckServiceTests` | 55 | 55 | 55 | 0 | 0 |
| 合計 | 56 | 56 | 56 | 0 | 0 |

Browser 測試本體 28.0 秒，SQL 測試本體 6 秒。兩次正式 run 均使用系統環境；Browser runner 與 provider fixture 只建立 allowlist 測試資料庫，完成後相關殘留數為 0。

## Artifacts 與資訊邊界

| Artifact | 保存 | Bytes | SHA-256 |
|---|---|---:|---|
| Playwright HTML report | 本機忽略目錄 `.run/e2e-rc-04-20260907/commit-779d1402/playwright-report/index.html` | 523,363 | `4a9a87b4a185ca3abaa56a032d5cacb985eff3508bf34c4ea117d4fb1b1fd2e6` |
| SQL TRX | 本機忽略目錄 `.run/e2e-rc-04-20260907/commit-779d1402/sql/e2e-rc-04-sql.trx` | 88,259 | `0dd8452a75438a815101c27631e4811da998a216ef33ad044066305c3d2991a1` |
| 去識別 T2 manifest | [[2026-09-07-E2E-RC-04測試證據清單.json]] | 版本控制 | 由 Git blob 保存 |

兩份 runner artifact 的 Authorization Header、Cookie Header、Private Key、JWT 與連線字串值均為 0 match；HTML 內三個 `password=` 為測試原始碼識別字指派，quoted credential value 為 0。通過時依 failure-only policy 不產生 trace、截圖或影片，且 raw artifact 不進版控。

## Delivery gate

- Local implementation review: 0 actionable finding。
- TypeScript `vue-tsc -b`、三個前端檔 ESLint、C# format、diff check：PASS。
- `BuildDetailPage.spec.ts`：22／22 PASS。
- Browser／SQL T2：PASS。
- Codex Security：待執行。
- PR Required CI／exact-head review／squash merge：待完成。
- E2E-RC-04／M-17 Browser Gate 尚未關閉；正式未完成追蹤表仍為 7 項，專案仍為 `NOT READY`。
