# DATA-RC-03｜展示資料驗證執行紀錄

- 日期：2026-09-07（Asia/Taipei）
- Base：`dev@3658a849ce36c51d75037ee35ca1f79df0def051`
- 程式 revision：`36f32a0087ba612b6ccaf905f00ef09db08f4d7a`
- FeatureProfile：`implemented-features-v2`
- 機器摘要：[[2026-09-07-DATA-RC-03展示資料驗證摘要.json]]

## 實作結果

`scripts/validate-demo-data.ps1` 呼叫 Development-only `--validate-demo`。Validator 不呼叫 `EnsureCreated`、`Migrate`、`SaveChanges` 或任何修正／清除流程；只允許本機／loopback SQL Server 及既有 Demo allowlist 名稱，並輸出彙總 JSON。

檢查範圍包含：Schema current、v2 Marker、10,000 筆總量、各 Entity 配額、特殊分布、FK 孤兒及 SQL constraints、負庫存、非法 enum、非法工作流，以及七份既有 Operational Report 的全部 Summary metrics。報表使用固定 Seed 期間與固定 as-of，避免長期未銷售指標隨系統日期漂移。

## 成功與故意失敗

| 路徑 | 實際結果 | 結論 |
|---|---|---|
| 既有乾淨 v2 Demo DB | 11／11 checks 為 true、完整性計數全為 0、exit 0 | PASS |
| 拋棄式 DB 將一筆 synthetic Paid PaymentAttempt 金額增加 1 | Counts、分布、約束及完整性仍通過，只有 `reportBaselines=false`、exit 1 | PASS（故意失敗被攔截） |
| 故意失敗 DB 清理 | 驗證後由外層 harness 刪除，readback 成功 | PASS |

本機命令：

```powershell
.\scripts\validate-demo-data.ps1 -DatabaseName DoSelectDemo_00000000000000000000000000000002
```

## 驗證證據

- Release solution build：0 warning／0 error。
- `dotnet format DoSelect.slnx --verify-no-changes --no-restore`：PASS。
- PowerShell parser：PASS。
- Focused provider-backed：3／3；TRX SHA-256 `448bd643782c5a3c2c02738dc02d9479e226d2bb83fdd9afdfa52faa88f9ac99`。
- 完整 Infrastructure：1,241／1,241；TRX SHA-256 `4b033dbe81acc9506cafced2a14f48e6c700d1dfa3984cbe58c27a148a8e21f9`。
- 成功輸出 SHA-256 `b12a3d1855cf21d8f36e936256e9e235ab8f86a4963acf3b2ed5754229565d9a`。
- 故意失敗輸出 SHA-256 `c3086e7ab16a19a25459962b3689fc621449021ccfaaaf3470a312406b11a188`。

原始 TRX／CLI 輸出只留在本機 `%TEMP%`，Repository 保存去識別摘要與 Hash。首輪沙箱 TLS 失敗，以及首輪 `sqlcmd` 因 SET 選項未真正注入污染的演練，均標成 invalidated，不用於通過判定。

## 安全與狀態

- 無 API、Schema、Migration、套件或 Production 執行路徑變更。
- JSON 不含連線字串、會員／地址／訂單／客服內容、Secret 或真實個資。
- 既有 `ReturnRequest.Priority` EF sentinel warning 如實保留，與本項變更無關。
- Security scan `f7a363ed-7c97-44d2-a4c8-f79e99fdfef2`：snapshot `cf89e056ac1ecf57f90b7c4955b083b498956b126815d1acf709fa422ed6cbfd`、7／7 review items、5 surfaces、0 finding、coverage `complete`。
- PR #147 exact head `473d9a4d752948d28582d716d7631dfb2fdd2747` 的 Required CI Run `34089858394` 全綠；Backend 12m11s、Browser E2E 4m59s、`CI Required` 通過。
- 同帳號正式 APPROVE 被 GitHub 拒絕後，依 alex 既有授權管理員 bypass squash merge；遠端 readback 為 `dev@483deeee830bbcb071143ee366cbc93ea3523899`。
- DATA-RC-03／DATA-08 已關閉；DATA-RC-04 改為下一個待辦。
