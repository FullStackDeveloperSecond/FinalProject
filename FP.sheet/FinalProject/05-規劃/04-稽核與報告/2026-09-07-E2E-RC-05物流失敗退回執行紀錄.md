# 2026-09-07 E2E-RC-05 物流失敗退回執行紀錄

## 結論

E2E-RC-05 已在 clean commit `443df7ed347a08eb9a75e1b432b15994ae4a259d` 完成本機 T2 驗證：一條真實 Admin／Customer UI→API→SQL Server 旅程 1／1 通過，`EfShipmentStatusServiceTests` 14／14 與 `ShipmentStatusApiTests` 6／6 通過、0 skipped。代表性高資料風險非主路徑為 COD 宅配訂單由 `InTransit` 進入 `DeliveryFailed`，再進入 `Returned`；兩次命令均要求原因，Shipment 與 Order fulfillment history 對齊，OrderStatus 維持原值，PaymentStatus 維持 `AwaitingPayment`、已付款 0、`PaidAtUtc=null`，且顧客端始終沒有發票。

本輪目前只完成本機固定 revision 的實作、review 與測試；Codex Security、公開 push、PR Required CI、exact-head review 與 squash merge 尚未完成，因此 E2E-RC-05／M-11 Browser Gate 仍維持進行中，專案仍為 `NOT READY`。

## Run contract

| 欄位 | 值 |
|---|---|
| Run ID | `e2e-rc-05-20260907-commit-443df7ed` |
| Revision | `443df7ed347a08eb9a75e1b432b15994ae4a259d`；正式 run 前後 tracked working tree clean |
| Calling Skill | `verify-vue-dotnet-e2e`；T2 evidence 由 `capture-test-evidence` 保存 |
| Browser | Playwright `1.62.1`、`admin-chromium`、1 worker、CI 設定 1 retry；實際 0 retry |
| Provider | .NET SDK `10.0.303`、本機 SQL Server、測試專屬資料庫 |
| 門檻 | Browser 與兩個 SQL 類別全部通過且 0 skipped；相關隔離資料庫全部清理 |
| 資料邊界 | deterministic synthetic 訂單／帳號；無 Production data、真實個資、Secret 或外部付費呼叫 |

## 最低成本範圍裁定

1. 主路徑與 Shipment 狀態服務、API、後台按鈕、歷程 UI 均已存在；文件或人工操作不足以形成可重跑的跨層證據，因此第一個充分方案是擴充既有 H-R02 Playwright 旅程。
2. 重用同一個已註冊 TOTP 的管理員及既有 COD 建單 helper，新增第三張合成宅配訂單；避免新增 seed 帳號，也避免平行測試競爭 TOTP 狀態。
3. 選擇 `InTransit → DeliveryFailed → Returned`，因錯誤收款、錯誤開票或誤改 OrderStatus 會直接破壞金流與訂單資料一致性；不新增產品 API、Schema、Migration、依賴或正式設定。
4. 真實 Checkout 的 COD 訂單在本流程維持 `Confirmed`；測試以失敗前讀回的 OrderStatus 作為不變量，不把 SQL fixture 的 `Processing` 假設硬套到真實 Browser 旅程。

## Journey 與 assertions

| 階段 | UI→API→Provider 輸入 | 預期／結果 |
|---|---|---|
| 建單與出貨 | 匿名 COD 宅配訂單；後台批次出貨後進入配送中 | PASS：顧客訂單可由 access token 讀取；初始未付款、無發票 |
| 配送失敗 | 正式後台「配送失敗」；`recipient_absent` 與去識別 note | PASS：未選原因時送出停用；Shipment／Fulfillment 為 `DeliveryFailed`；兩層歷程含相同轉換，Order fulfillment history 另含原因 |
| 退回商家 | 正式後台「退回商家」；`recipient_refused` 與去識別 note | PASS：未選原因時送出停用；Shipment／Fulfillment 為 `Returned`；可用命令清空；兩層歷程對齊 |
| COD／發票不變量 | 失敗與退回後由管理端 API 與顧客 UI／API 讀回 | PASS：OrderStatus 不變、`AwaitingPayment`、已付款 0、`PaidAtUtc=null`、invoice 404 |
| Provider regression | Shipment service 與 Shipment API 兩個 SQL 類別 | PASS：20／20、0 skipped；涵蓋狀態機、理由、RowVersion、授權、歷程與副作用 |

## Feedback loop

| 輪次 | 觀察 | 最小修正 | 重驗 |
|---|---|---|---|
| 1 | 聚焦標題改為 E2E-RC05 後，runner 未啟用既有 E2E simulation endpoint，H-R02 的提前完成衝突檢查由預期 409 變成 404 | 將 `*E2E-RC05*` 加入既有、仍受 `ASPNETCORE_ENVIRONMENT=E2E` 與隨機 allowlist DB 限制的旅程 allowlist | 原 404 環境誤配消失 |
| 2 | 新增斷言把 SQL fixture 的 `Processing` 當成真實 Checkout 狀態，但 Browser 建立的 COD 訂單正確維持 `Confirmed` | 在 `InTransit` 後讀回 OrderStatus，後續只斷言配送失敗與退回不得改變它 | Browser 正式 run 1／1 PASS |

兩次失敗均保留為診斷紀錄，不冒充通過證據；每次 runner 都在 finally 移除該輪隨機 E2E 資料庫。

## Execution

| Command | Discovered | Executed | Passed | Failed | Skipped |
|---|---:|---:|---:|---:|---:|
| `test-customer-e2e.ps1 -Project admin-chromium -JourneyTitle E2E-RC05` | 1 | 1 | 1 | 0 | 0 |
| `dotnet test DoSelect.Infrastructure.Tests.csproj --filter FullyQualifiedName~EfShipmentStatusServiceTests` | 14 | 14 | 14 | 0 | 0 |
| `dotnet test DoSelect.Api.IntegrationTests.csproj --filter FullyQualifiedName~ShipmentStatusApiTests` | 6 | 6 | 6 | 0 | 0 |
| 合計 | 21 | 21 | 21 | 0 | 0 |

Browser 測試本體 35.8 秒，兩個 SQL 類別測試本體合計 4 秒。正式 run 使用系統環境；Browser runner 與 provider fixtures 只建立 allowlist 測試資料庫，完成後相關殘留數為 0。API 測試顯示 NuGet 弱點服務索引暫時不可達的 `NU1900` warning；套件已還原且 6／6 測試通過，正式 Package 稽核仍保留為 PR Required CI Gate。

## Artifacts 與資訊邊界

| Artifact | 保存 | Bytes | SHA-256 |
|---|---|---:|---|
| Playwright HTML report | 本機忽略目錄 `.run/e2e-rc-05-20260907/commit-443df7ed/browser/playwright-report/index.html` | 540,831 | `6dc637e99a25f92d73cb9e16622a0b711ad926b39c08c03b9fb017c57d06835e` |
| Infrastructure TRX | 本機忽略目錄 `.run/e2e-rc-05-20260907/commit-443df7ed/infrastructure/E2E-RC05-Infrastructure.trx` | 24,121 | `f8a655031904343cf2436099efb512b72a9c5a84d219c6b362a8fdb05dfbe248` |
| API TRX | 本機忽略目錄 `.run/e2e-rc-05-20260907/commit-443df7ed/api/E2E-RC05-Api.trx` | 51,154 | `792918e5e3c053bb0253b4270a5b9232488e6358f1c9b71ab3ac06e9eff5946b` |
| 去識別 T2 manifest | [[2026-09-07-E2E-RC-05測試證據清單.json]] | 版本控制 | 由 Git blob 保存 |

三份 runner artifact 的 Authorization Header、Cookie Header、Private Key、JWT、連線字串值與 quoted credential value 均為 0 match；HTML 內三個 `password=` 為測試原始碼識別字指派。本輪未設定 `VISUAL_REVIEW_DIR`，因此 `captureVisualEvidence` 為 no-op，沒有產生或提交截圖；本次 T2 只保留 HTML 與 TRX。

## Delivery gate

- Local implementation review：0 actionable finding。
- TypeScript `vue-tsc -b`、聚焦 ESLint、PowerShell parse、diff check：PASS。
- Browser／SQL T2：PASS，21／21、0 skipped、0 retry。
- 尚待 Codex Security、公開 push、PR Required CI、exact-head review、正式 APPROVE 嘗試與 squash merge。
- Gate 完成前 E2E-RC-05／M-11 Browser Gate 不勾選；正式未完成追蹤表仍為 7 項，專案維持 `NOT READY`。
