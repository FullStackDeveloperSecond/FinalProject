# 2026-09-07 E2E-RC-03 自由組裝主旅程執行紀錄

## 結論

E2E-RC-03 在 commit `c219090d73c2d5b063ac13828b82253dd034dd6a` 完成本機 T2 驗證：一條顧客 UI→API→SQL Server 主旅程 1／1 通過，`EfBuildListServiceTests` 與 `CompatibilityCheckServiceTests` 合計 55／55 通過、0 skipped。會員從首頁自由組裝入口選齊 8 類相容零件，看到相容結果後保存清單、建立真實分享 URL 並由全新匿名 context 開啟，再將原清單整套加入購物車；官方 API 與購物車 UI 證明只新增 1 個 8 品項組裝群組與 NT$300 組裝費。失效、撤銷、刪除及停權擁有者的分享 token 均由 SQL provider assertions 收斂為相同 `resource_not_found`，不洩漏原因。PR #154 再通過完整 Security、Required CI 與 exact-head review，於 `2026-09-07T11:11:00Z` squash merge 為 `dev@1efd0e46`；E2E-RC-03／M-16 Browser Gate 已完成。

## Run contract

| 欄位 | 值 |
|---|---|
| Run ID | `e2e-rc-03-20260907-commit-c219090d` |
| Revision | `c219090d73c2d5b063ac13828b82253dd034dd6a`；兩組正式 run 前 tracked working tree clean |
| Calling Skill | `verify-vue-dotnet-e2e`；T2 evidence 由 `capture-test-evidence` 保存 |
| Browser | Playwright `1.62.1`、`customer-chromium`、1 worker、CI 設定 1 retry；實際 0 retry |
| Provider | .NET SDK `10.0.400`、本機 SQL Server、測試專屬資料庫 |
| 門檻 | 選定 Browser 與 SQL 類別全部通過，且 0 skipped；建立的相關隔離資料庫全部清理 |
| 資料邊界 | 既有 deterministic synthetic catalog／帳號；無 Production data、真實個資、Secret 或外部付費呼叫 |

## 最低成本範圍裁定

1. M-16 的首頁入口、組裝精靈、相容性服務、保存、分享與整套加入購物車均已存在，文件或人工操作不能提供可重跑的跨層證據；第一個充分方案是只補一條 Playwright 主旅程。
2. 旅程重用既有 8 類 deterministic compatible SKU，不新增 seed、產品 API、DTO、Schema、Migration、套件或正式產品行為。
3. 後端原先以預設 `localhost:5173` 產生分享 URL，但 E2E web server 固定在 `127.0.0.1:5173`；只在 Playwright 啟動的後端環境設定 `Frontend__BaseUrl`，使測試直接開啟的真實 URL 與實際 origin 一致。
4. 失效分享的不同內部原因已由相同 production service 的 provider tests 證明全部回傳 `resource_not_found`；不為四種等價原因重複新增 Browser journey。相容性阻擋／缺證據的 Browser 類型仍屬 E2E-RC-04。

## Journey 與 assertions

| 步驟 | 正式邊界 | 預期／結果 |
|---|---|---|
| 會員登入並從首頁按「自由組裝」 | Customer Vue UI／Router | PASS：進入 `/builds/new` |
| 依序選 CPU、主機板、記憶體、顯示卡、儲存、電源、機殼、散熱器 | 正式商品搜尋 UI→API→SQL Server | PASS：8 類 deterministic SKU 均選入 |
| 相容性與保存 | UI→Compatibility API／Build List API→SQL Server | PASS：`compatible`；保存後 GET 名稱正確且 8 items |
| 建立分享並以全新匿名 context 直接開啟 URL | UI→Share API→匿名 Shared Build API→SQL Server | PASS：相同名稱、8 items、`compatible` |
| 將擁有者清單整套加入購物車 | UI→Add-to-cart API→SQL Server→Cart UI | PASS：恰新增 1 assembly group、8 items、assembly fee 增加 NT$300 |
| 失效分享保密 | SQL Server production service assertions | PASS：舊 token、撤銷、刪除、未知及擁有者停權皆為 `resource_not_found` |

## Execution

| Command | Discovered | Executed | Passed | Failed | Skipped |
|---|---:|---:|---:|---:|---:|
| `test-customer-e2e.ps1 -Project customer-chromium -JourneyTitle <E2E-RC-03 title>` | 1 | 1 | 1 | 0 | 0 |
| `dotnet test DoSelect.Infrastructure.Tests.csproj --filter FullyQualifiedName~EfBuildListServiceTests\|FullyQualifiedName~CompatibilityCheckServiceTests` | 55 | 55 | 55 | 0 | 0 |
| 合計 | 56 | 56 | 56 | 0 | 0 |

Browser 測試本體 17.0 秒，SQL 測試本體 6 秒。兩次正式 run 均使用系統環境；Browser runner 與 provider fixture 都只建立隨機 allowlist 測試資料庫，完成後查得相關殘留數 0。

## Artifacts 與資訊邊界

| Artifact | 保存 | Bytes | SHA-256 |
|---|---|---:|---|
| Playwright HTML report | 本機忽略目錄 `.run/e2e-rc-03-20260907/commit-c219090d/playwright-report/index.html` | 521,467 | `7ecbcdcaf875f851be749f3d7e63651048c2172dda46c61ded8664bbd04ff4d4` |
| SQL TRX | 本機忽略目錄 `.run/e2e-rc-03-20260907/commit-c219090d/sql/e2e-rc-03-sql.trx` | 88,259 | `a309f5916210cbadadeb4495ad33749901b515acd43b044817a7ac90c7c03b9a` |
| 去識別 T2 manifest | [[2026-09-07-E2E-RC-03測試證據清單.json]] | 版本控制 | 由 Git blob 保存 |

兩份 runner artifact 的 Authorization／Cookie、Private Key、JWT、連線字串值與 quoted credential value 均為 0 match。HTML 內有 17 個 `password=` 類型的測試原始碼識別字指派，沒有 quoted credential value；不將識別字誤報為祕密。通過時依 failure-only policy 不產生 trace、截圖或影片。

## Delivery gate

- Local implementation review: 0 actionable finding。
- TypeScript `vue-tsc -b`: PASS。
- Spec／Playwright config ESLint: PASS。
- Browser／SQL T2: PASS。
- Codex Security `00be63a4-c7ab-4c2d-89bc-75e1c8d4dbec`: coverage complete、6／6、0 finding／deferred。
- PR #154 Required CI Run `34114063138`: PASS；Backend 12m37s、Browser 4m55s、雙 Frontend 1m06s／1m13s。
- Exact-head review: `1be68c03cb23dd4ed466fc1548d8f909fd55ba2d`、0 comment／inline comment／review finding。
- 正式 APPROVE 因 GitHub 禁止同帳號自我核准而被拒絕；所有 Gate 通過後依 alex 既有授權管理員 squash merge，`dev@1efd0e46`。
- E2E-RC-03／M-16 Browser Gate 已完成；正式未完成追蹤表仍為 7 項，專案仍為 `NOT READY`。
