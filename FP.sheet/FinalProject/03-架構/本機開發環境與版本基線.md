---
文件狀態: 部分已確認
最後更新: 2026-08-12
追蹤項目:
  - DEV-02
  - DEV-03
  - DEV-04
  - DEV-05
---

# 本機開發環境與版本基線

## 已確認版本

| 能力 | 版本／工具 | 鎖定方式 |
|---|---|---|
| Backend | .NET 10 SDK、ASP.NET Core 10 | `global.json` 待建立 |
| Database | SQL Server 2025 Developer Edition；Instance `.\SQL2025`；Database `DoSelectDb` | 安裝文件與連線設定待建立 |
| Frontend | Node.js 24 LTS | 根目錄 `.nvmrc`／版本檔固定 Major |
| Package Manager | npm | 前後台提交各自 `package-lock.json`，不得混用 pnpm／Yarn |
| Source Control | Git＋GitHub | [[Git協作規範]] |

## 新電腦安裝順序

1. 安裝 Git、.NET 10 SDK、Node.js 24 LTS、SQL Server 2025 Developer Edition 與管理工具。
2. Clone Repository；第一版目前不使用 Git LFS 或 Docker。
3. 依範例建立 User Secrets／本機環境設定，不把 SQL、OpenAI 或 Brevo Secret 寫進 Repository。
4. 在兩個 Vue 專案分別使用 `npm ci`，不得以 `npm install` 任意改寫鎖檔後直接提交。
5. Restore／Build .NET Solution，建立本機資料庫並套用 EF Core Migration。
6. 產生固定 Seed 展示資料，啟動 API、customer-web、admin-web。
7. 執行 Readiness、基本登入、商品列表及資料庫版本 Smoke Check。

## 版本檢查目標

最終安裝文件必須提供不修改系統狀態的檢查指令，至少確認：

- `dotnet --version` 符合 `global.json`。
- `node --version` 為 Node 24 LTS Major。
- `npm --version` 符合團隊記錄版本。
- SQL Server Instance 可連線且版本／Edition 正確。
- `npm ci`、.NET Restore、Build、Migration 與測試可由乾淨環境重現。

## 套件管理規則

- customer-web 與 admin-web 都使用 npm；不得一個專案使用 npm、另一個使用 pnpm。
- `package.json` 與 `package-lock.json` 必須同一個 PR 更新。
- CI 使用 `npm ci`；鎖檔不一致時直接失敗。
- Node Major 升級、套件管理器更換或鎖檔重建需獨立 PR，附兩個 Vue 專案的 Lint、Typecheck、Test 與 Build 結果。

## 尚待落實

- 建立實際 `global.json`、`.nvmrc`、Solution 與兩個 Vue 專案後，填入精確命令與路徑。
- SQL Server Instance 已定為 `.\SQL2025`、資料庫名稱已定為 `DoSelectDb`；驗證模式及團隊連線字串範例仍待確認。
- 建立不含 Secret 的設定範本及 User Secrets Key 清單。
- 完成 `start-all.ps1`、`stop-all.ps1`、`status.ps1`、`reset-demo-data.ps1`、`health-check.ps1`。
- 由一台未設定過的電腦依文件完成一次安裝驗證並記錄時間與問題。
