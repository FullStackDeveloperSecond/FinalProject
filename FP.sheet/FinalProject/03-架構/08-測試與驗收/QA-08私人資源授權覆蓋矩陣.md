---
文件狀態: 第一階段已驗證，隨 Endpoint 持續更新
最後更新: 2026-08-26
追蹤項目:
  - QA-08
  - DEV-07
---

# QA-08 私人資源授權覆蓋矩陣

## 文件目的

本矩陣記錄目前已實作 Endpoint 的 Actor A／Actor B、匿名／錯誤角色與拒絕後無副作用證據。它不是未來 Endpoint 的預先驗收結果；新增私人資源、寫入、刪除、附件、匯出、批次或背景操作時，必須在同一個 PR 增列矩陣與測試。

本文件依 [[03-架構/04-安全與檔案/安全與供應鏈強制驗收標準]] 的 SEC-ACC-01～03 執行。

## Actor 定義

| 名稱 | 定義 |
|---|---|
| Actor A | 資源擁有者，或規則明確允許的管理角色／指派處理人 |
| Actor B | 另一位會員、另一個訪客購物車 Key、錯誤管理角色，或不在案件指派範圍內的管理員 |
| 未登入者 | 沒有有效會員／管理員 Session；訪客購物車則以缺少 Guest Cart Key 表示沒有資源憑證 |
| 無副作用 | 拒絕後資料列、數量、狀態、RowVersion、歷程、附件中繼資料及私有檔案維持不變 |

訪客購物車 Key 是該訪客購物車的持有憑證，因此訪客 A／B 隔離以不同 Key 驗證；取得同一 Key 的呼叫者視為持有同一訪客購物車憑證。

## 現行覆蓋

| 資源／能力 | Actor A／授權角色成功 | 未登入／錯誤角色 | Actor B／Scope 隔離 | 拒絕後無副作用 | 主要自動化證據 | 狀態 |
|---|---|---|---|---|---|---|
| 會員客服案件列表與明細 | 建立後可查明細；A 列表含自己的案件 | Controller Policy 驗證 401 | B 明細為一致 404；B 列表不含 A 案件 | 純讀取，不改狀態 | SupportTicketsControllerTests.CreateThenGet_WhenAuthenticated_RoundTripsTicketWithComputedSla、GetDetail_WhenTicketBelongsToAnotherMember_Returns404NotForbidden、List_WhenTicketBelongsToActorA_DoesNotExposeItToActorB | ✅ |
| 客服案件新增訊息 | Application 成功路徑與 HTTP 併發／驗證契約已有測試 | 由會員 Policy 統一攔截 | B 使用 A Ticket PublicId 回 404 | 案件狀態、RowVersion、訊息數不變 | SupportTicketsControllerTests.AddMessage_WhenActorBUsesActorATicketId_Returns404WithoutSideEffects | ✅ |
| 客服案件取消 Action | A 在允許狀態可取消 | 由會員 Policy 統一攔截 | B 使用 A Ticket PublicId 回 404 | 狀態、RowVersion、狀態歷程數不變 | SupportTicketsControllerTests.Cancel_WhenOpenWithNoReply_TransitionsToCancelled、Cancel_WhenActorBUsesActorATicketId_Returns404WithoutSideEffects | ✅ |
| 客服附件上傳 | A 經 HTTP 上傳並可安全讀回原始位元組 | 匿名 Multipart 回 401，Application 不被呼叫 | B 對 A Ticket PublicId 回 404 | 附件資料列為 0；private 暫存與最終目錄無檔案 | SupportAttachmentUploadRoundTripTests.UploadThroughHost_ThenSecureReadService_ReturnsExactOriginalBytes、UploadThroughHost_WhenActorBTargetsActorATicket_Returns404WithoutDatabaseOrFileSideEffects | ✅ |
| 私有客服附件讀取 | 擁有者與符合案件範圍的處理人可讀 | 匿名與不存在資源均使用一致 404 | 會員 B、已刪除、非 Clean、範圍外管理員均不可讀 | 拒絕時不開啟 Storage | SupportAttachmentReadStoreSqlServerTests.SqlPredicate_AllowsOwnerAndHandlerButNotMemberBDeletedOrNonClean、PrivateAttachmentsHttpAcceptanceTests.AnonymousAndMissing_AreEquivalent404WithoutRedirectOrServiceCall | ✅ |
| 訪客購物車讀取 | A Key 只取得自己的購物車 | 缺少會員 Session 與 Guest Key 回 400 validation_failed | B 使用不同 Key 不會看到 A 商品 | 純讀取，不改 A 資源 | CartApiTests.GetCart_WhenActorBUsesAnotherGuestKey_DoesNotExposeActorAItems | ✅ |
| 訪客購物車修改／刪除商品 | A Key 可依 RowVersion 修改／刪除自己的商品 | 缺少資源憑證由共通 Identity 驗證拒絕 | B Key 使用 A Item PublicId 回 404 | 數量、Cart／Item RowVersion 與 Actor A 資料列不變；Cart 總筆數不增加 | CartApiTests.UpdateItemQuantity_WhenActorBUsesActorAItemId_Returns404WithoutSideEffects、RemoveItem_WhenActorBUsesActorAItemId_Returns404WithoutSideEffects | ✅ |
| 會員購物車讀取 | A Session 只取得自己的購物車 | 無 Session 且無 Guest Key 回 400 | 會員 B 不會看到 A 商品 | 純讀取，不改 A 資源 | CartApiTests.GetCart_WhenActorBIsAnotherMember_DoesNotExposeActorAItems | ✅ |
| 會員購物車修改／刪除商品 | A Session 可依 RowVersion 修改／刪除自己的商品 | 無有效 Identity 時由共通驗證拒絕 | 會員 B 使用 A Item PublicId 回 404 | 數量、Cart／Item RowVersion 與 Actor A 資料列不變；Cart 總筆數不增加 | CartApiTests.UpdateItemQuantity_WhenActorBIsAnotherMember_Returns404WithoutSideEffects、RemoveItem_WhenActorBIsAnotherMember_Returns404WithoutSideEffects | ✅ |
| AI 客服引用會員訂單 | 已授權會員可在同意與額度規則下取得自己的去識別化訂單內容 | 匿名 401、錯誤帳號型別 403 | 引用非本人訂單回 404 | 不保留額度且不呼叫模型 | AiSupportEndpointTests.UnownedReferencedOrder_Returns404WithoutReservingOrCallingModel、AuthorizedSafeRequest_PropagatesLocaleAndOrderContextAndReservesOnce | ✅ |
| 後台客服明細、Claim、SLA 與工作台 | 符合客服角色及案件範圍者可操作 | 匿名 401；SuperAdmin 單獨或 Member 角色回 403 | 一般客服受 Assignee Scope；主管依既定 Scope 查看；Cursor 綁定管理員與 Scope | 失敗 Claim 不新增歷程、不改案件；交易失敗全回滾 | SupportPolicyHttpAcceptanceTests、AdminSupportTicketDetailStoreTests、AdminSupportTicketClaimStoreTests、M14BReadModelAcceptanceTests | ✅ |
| 後台商品建立／修改 | CatalogManager／SuperAdmin 可依各資源契約建立或修改品牌、分類、標籤、商品與 SKU | 每個 POST／PUT 路由均驗證匿名 401、OrderManager 403 | 第一版單一商家，以 CatalogManager Policy 為 Scope；不虛構 Merchant Scope | 拒絕前後 Brands、Categories、Tags、Products、Skus 與 ProductTags 的 PublicId／RowVersion／關聯快照完全相同 | CatalogAdminAuthorizationTests.WriteEndpoint_RejectsAnonymousAndWrongRoleWithoutCatalogSideEffects（10 個路由案例） | ✅ |
| 後台退款折讓 | 完成 MFA 的 FinanceManager／SuperAdmin 可對正確發票與成功退款開立折讓 | 匿名 401、OrderManager 403、FinanceManager 未完成 MFA 403 | 路由 Invoice PublicId 與 Body Refund PublicId 必須屬同一交易；交叉 Parent ID 回一致 404 | 拒絕後折讓表頭／明細、Audit、Idempotency 均為 0，原發票與交叉發票狀態／RowVersion 不變 | AdminInvoiceAllowancesControllerTests、InvoiceAllowanceWriterSqlServerTests.RefundForAnotherInvoiceIsRejectedWithoutDatabaseSideEffects | ✅ |
| 後台 SKU 刪除 | CatalogManager 可刪除 Draft 且無參照 SKU | 匿名 401；OrderManager 403 | 單一商家 SKU 不適用會員 Owner 隔離，以角色 Policy 為 Scope | 未授權時 SKU 與 RowVersion 不變；有庫存／訂單／圖片等參照或 RowVersion 過期時不刪除 | CatalogAdminAuthorizationTests.Anonymous_DeleteSku_Returns401AndDoesNotDeleteResource、SignedInWithoutCatalogManagerRole_DeleteSku_Returns403AndDoesNotDeleteResource、SkuAdminApiTests.Delete_WhenDraftAndUnreferenced_Returns204 | ✅ |

## 本批新增證據

2026-08-25 新增 13 個 SQL Server-backed 案例：

- 購物車：訪客 A／B 三案、會員 A／B 三案。
- 客服：案件列表隔離、跨會員新增訊息、跨會員取消、跨會員附件上傳。
- SKU 刪除：匿名與錯誤角色各一案，皆驗證資源與 RowVersion 不變。
- 過期購物車：呼叫者既有 Cart 已逾期時，修改商品回 404，Cart／Item 快照與 Cart 總筆數完全不變。

原始四個整合測試類別共 56 案通過；另新增 1 個 CartService 過期拒絕案例。完整 Solution 建置為 0 警告、0 錯誤，Domain 194、Application 177、Infrastructure 262、API Integration 279，共 912 案全數通過。

依 DEC-P308，四個購物車修改／刪除案例再加入 Cart 總筆數快照：修正前四案均由 N 增為 N+1 而失敗；改為只查既有 Cart 後，使用 SDK 10.0.303 與 SQL Server 的 Actor B focused filter 共 6／6 通過。另以 CartService provider-backed 案例驗證呼叫者既有 Cart 已逾期時，拒絕修改不會改狀態、到期時間、UpdatedAt、RowVersion、Item 或總筆數，focused 1／1 通過。本次修正後完整 Solution Build 0 警告／0 錯誤、Format 通過，Domain 194、Application 177、Infrastructure 262、API Integration 279，共 912／912 通過。

## 2026-08-26 現行 Endpoint 基線補強

- 後台商品：以 10 個 SQL Server-backed Theory 案例逐一覆蓋品牌、分類、標籤、商品、SKU 的 POST／PUT；每案各送出匿名與 OrderManager 請求，分別取得 401／403，且六組 Catalog 資料快照完全不變。
- 後台退款折讓：新增 1 個 SQL Server provider-backed 交叉 Parent ID 案例；以發票 B 搭配退款 A 時回 resource_not_found，折讓表頭／明細、Audit、Idempotency 均不寫入，兩張發票狀態與 RowVersion 不變。
- 分支已 rebase 至最新 dev；SDK 10.0.303、Build 0 警告／0 錯誤、Format 通過。Focused 測試為商品 10／10、退款折讓 1／1；完整 Solution 為 Domain 314、Application 202、Infrastructure 317、API Integration 303，共 1136／1136 通過、略過 0。
- CI 等價路徑已驗證：`DOSELECT_SQLSERVER_TEST_CONNECTION` 指向共用 SQL Server，API／Infrastructure fixture 強制改用各自專屬資料庫名稱；未設定環境變數時保留 `.\SQL2025` 本機 fallback。
- 現行已實作私人 Endpoint 的本機證據缺口已補齊；正式完成仍以本分支 Required CI、Review 與 Merge 為 Gate。

## 不適用與尚待完成

| 項目 | 目前判定 |
|---|---|
| SKU 跨商家 Scope | 第一版是單一商家，沒有 Merchant Scope，因此不虛構跨商家測試；仍以 CatalogManager Policy 驗證 |
| SKU 批次刪除 | 目前沒有批次刪除 Endpoint，SEC-ACC-03 的批次混入未授權列案例不適用 |
| 客服附件刪除 | 目前沒有附件刪除 Endpoint；未來新增時必須驗證資料列與實體檔同成同敗 |
| 訂單、退貨、退款的其他能力、收藏、通知、匯出及其他未完成私人 Endpoint | 不在本批程式基線內；各 Owner 實作時必須新增 Actor A／B 與無副作用案例 |
| Linux Required CI | 依 DEC-P307 沿用既有 SQL Server 2025 service；已移除 `Category!=RequiresSqlServer` 排除條件、設定 `DOSELECT_SQLSERVER_TEST_CONNECTION`，並讓固定資料庫 fixture 支援 CI 連線且彼此隔離。本機 CI 等價驗證 1136／1136、略過 0；仍須取得本分支 Required CI 成功證據 |
| Package 來源證據 | PR #41 已由 Run `32851496154` 完整通過並 Squash Merge 至 `dev`，Merge Commit `64b0ef4` |
| QA-08 整體狀態 | 維持進行中；現行已實作私人 Endpoint 的本機基線已補齊且 1136／1136 通過，本分支尚待 Required CI／Review／Merge；未來 Endpoint 仍須持續補表 |

## 更新規則

1. 新增或修改私人 Endpoint 的 PR 必須同步更新本矩陣。
2. 每個寫入能力至少新增 Actor B 以 A 的 Route／Parent／Body／Action 識別值呼叫的負面案例，並查核資料庫與外部副作用。
3. 每個刪除能力需包含成功、匿名、錯誤角色／其他使用者、非法狀態、併發；只有實際存在批次能力時才加入批次混入案例。
4. SQL Server 測試必須維持在 Required CI 的無過濾 `dotnet test` 內；Workflow 或測試分類變更不得使 `RequiresSqlServer` 案例靜默退出合併 Gate。
