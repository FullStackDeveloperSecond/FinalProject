---
文件狀態: 已確認
最後更新: 2026-08-13
追蹤項目: [DEMO-03]
---

# 故障降級與復原手冊

| 故障 | 立即判斷 | 台上降級 | 台下復原 |
|---|---|---|---|
| OpenAI 逾時／限流 | API 回穩定 AI 錯誤且基本搜尋正常 | 展示關鍵字搜尋，再播放 AI 片段 | 檢查額度、網路、模型設定與 Outbox／Log |
| Brevo／Email 失敗 | 商業交易成功、通知 Job Failed | 顯示站內通知與寄送嘗試紀錄 | 具理由人工重送，不重建訂單 |
| Customer Web 故障 | API／Admin 正常 | 播放前台預錄，後台接續現場 | 重啟單一前端，不終止所有 Node |
| Admin Web 故障 | API／Customer 正常 | 播放後台預錄，報表用截圖 | 重啟 Admin Web |
| API 不健康 | Ready 失敗 | 立即切完整預錄 | 保存 Log／Trace，檢查設定、Port、SQL，不在台上修改資料 |
| SQL Server 不可用／資料損壞 | Ready 失敗或不變量錯誤 | 立即切完整預錄 | 還原至獨立 DB 驗證後切換，不直接覆寫唯一資料庫 |
| TOTP／帳號失敗 | 無法完成管理登入 | 使用已登入但最小權限的備用 Profile；無則播片 | 檢查時間同步、恢復碼與帳號鎖定，不關閉 2FA |
| Seed 被現場操作改變 | 固定案例不存在 | 切換預先準備的第二案例 | 執行受控 Reset 前先備份，重新跑資料驗證 |
| 付款／物流模擬失敗 | 狀態衝突或回呼未處理 | 使用預先準備在下一狀態的訂單 | 查 Idempotency／Outbox，不手改 SQL 狀態 |

## 禁止事項

- 不在展示中修改資料庫、Secret、Prompt 或角色。
- 不為了成功而停用授權、2FA、Antiforgery、個資遮蔽或相容性規則。
- 不刪除 Failed Job、AuditLog 或錯誤證據來讓畫面變乾淨。
