---
文件類型: 開發日誌
建立時間: 2026-08-31 05:10
作者: yinyin
負責模組: M-07 優惠券（A-23 目錄參考端點前置）
狀態: 待覆核
相關分支: feature/coupon-catalog-options
相關 Commit/PR: 待建立
---

# 開發日誌

## 作者與負責範圍

| 欄位 | 內容 |
|---|---|
| 作者 | yinyin（以 Catalog 備援身分實作） |
| 負責模組 | 優惠券挑選器的目錄參考端點（PR #64 P2#3 前置） |
| 本次工作範圍 | 契約、Catalog 側 Reader、端點、DI、OpenAPI／typed client、provider-backed 測試 |
| 不在本次工作範圍 | #64 的前端串接（等本支合併後 rebase 再做） |
| Catalog domain review | 待 alex 指派 |
| 狀態 | 待覆核 |

## 依據

alex 2026-08-29 於 PR #64 的 P2#3 裁定：**不放寬 `CatalogManager`**（那會把
FinanceManager／MarketingAnalyst 的權限擴張到目錄管理邊界），也不建立通用的高權限
參考端點；改用受 `Coupon.Manage` 保護、用途限定的批次唯讀端點，拆成獨立前置 PR。

## 我上一輪錯在哪

我當時只在問「哪個端點的權限過得去」，所以選了公開的 `/api/v1/catalog/filter-options`
與 `/api/v1/products`。**沒有算過它會放大成多少次查詢**：分類樹每個節點各打一次
（上限 100），而那個端點每次還會順便算品牌、價格區間與規格篩選；已選商品又逐筆查明細，
兩個 picker 各最多 50 次。一次普通編輯就是上百次 HTTP 與 SQL。

「權限過得去」和「代價可接受」是兩個問題，我只回答了第一個。

## 交付

| 位置 | 內容 |
|---|---|
| `DoSelect.Application/Catalog/CouponCatalogOptionsContracts.cs` | DTO、規則常數、`ICouponCatalogOptionsReader` |
| `DoSelect.Infrastructure/Catalog/CouponCatalogOptionsReader.cs` | 三個查詢，各一次往返 |
| `DoSelect.Api/Promotions/AdminCouponCatalogOptionsController.cs` | `/api/v1/admin/coupons/catalog-options`，`Coupon.Manage` |

契約與 Reader 放 Catalog-owned 位置；路由掛在 Promotions 是因為授權與用途都屬優惠券。

## 幾個實作決定

**批次解析用 `POST` 而不是 `GET`。** 200 個 GUID 塞進 query string 會超過多數反向代理的
URL 長度上限，而且 `PublicId` 不該進網址（會留在存取紀錄裡）。這支沒有副作用，只是查詢。

**分類整棵樹一次撈回來再在記憶體接路徑。** 分類是參考資料、量級數十筆，
用遞迴 CTE 或逐層查詢換來的複雜度買不到什麼。接路徑時有一個走訪上限，
資料若出現環狀 parent 才不會在那裡無限繞。

**搜尋多取一筆判斷 `hasMore`**，不另外打一次 `COUNT`。

**`ToOption` 的 `switch` 對未知狀態丟例外**，不預設成「可選」——
那會讓一個沒人審過的新狀態自動取得排進優惠券的資格。

**超量批次丟例外而不是截斷。** 靜默截斷會讓被切掉的那幾筆看起來像「這個商品不存在」，
而呼叫端正要用它來顯示既有規則。

## 測試

十條 provider-backed（真實 SQL Server）。核心是 alex D1 的驗收條件：

> provider-backed 測試要斷言多筆輸入維持固定、有限的 SQL 往返，不得隨選取數量形成 N+1。

`ResolvingManyProductsTakesTheSameOneRoundTripAsResolvingOne` 用 `DbCommandInterceptor`
數實際命令數：解析 1 筆與解析 25 筆都必須是 **1 個命令**。
只斷言「回傳內容正確」不夠 —— 逐筆查也會回傳正確內容。

破壞式驗證：把批次解析改成迴圈裡逐筆查，這條立刻變紅。

四種 `ProductStatus` 的 `IsSelectable` 全部列出來；停售商品「解析得到但搜不到」
分成兩條測試，因為那是兩個不同的性質。

## 驗證

| 項目 | 結果 |
|---|---|
| 零警告建置 | 0 警告 0 錯誤 |
| Infrastructure 測試 | 605／605（新增 10） |
| Api Integration 測試 | 533／533 |
| admin-web typecheck／測試 | 通過，156／156 |
| `dotnet format` | 通過 |
| OpenAPI 與 typed client | 已重產並提交 |
| Host 啟動 | 已實測（DI 驗證在 host build 期，缺註冊會弄垮整個 API） |
