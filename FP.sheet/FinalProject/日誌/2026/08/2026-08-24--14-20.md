# 2026-08-24 客服模組開發日誌

## 今日完成

- 將 `feature/support-tickets` rebase 至組長最新 `dev`，並保留客服模組既有成果。
- 統一 `Storage:DataRoot` 設定來源，修正附件上傳與下載使用不同根目錄而造成 HTTP 500 的風險。
- 會員端與管理端案件詳情新增安全附件清單及權限保護下載連結；只回傳 Clean、未刪除附件的公開欄位。
- 會員客服案件列表新增伺服器端分頁與篩選後回到第一頁的行為。
- 補強建立案件、回覆與取消輸入驗證：拒絕缺少/數字 enum、純空白主旨、訊息及取消原因。
- 更新完整 OpenAPI 契約與共用 TypeScript schema；管理端客服 API 改用生成的 typed client。
- CI 新增 OpenAPI 契約漂移檢查，避免後端契約更新後前端型別未同步。

## 驗證結果

- Backend solution build：通過，0 warnings / 0 errors。
- Domain tests：182/182 通過。
- Application tests：127/127 通過。
- Customer frontend：typecheck、lint、30/30 tests、production build 全部通過。
- Admin frontend：typecheck、lint、16/16 tests、production build 全部通過。
- OpenAPI export / generation：通過。
- 本機 SQL Server 測試仍受既有環境限制阻擋：目前電腦不支援 SQL Server 要求的加密連線；此項由 GitHub Actions 的 SQL Server service 驗證。
- `dotnet format --verify-no-changes` 本機工具程序無法解析企劃指定的 SDK 10.0.303；一般 build 使用已安裝 preview SDK 可通過，CI 會依 `global.json` 使用 10.0.303 再驗證。

## 後續工作

- 觀察本次 push 的 Backend / Frontend CI 結果並處理實際失敗。
- 第一優先基本功能完成後，進入 M-12 退貨、寄回、檢查及退款交接。
- 前台畫面後續調整仍遵守：正式改 UI 前先提供示意圖並取得確認。

## 需協調事項

- 目前沒有新增需組長裁定的需求衝突；本次修改依 PR #10 的既有審查意見完成。
