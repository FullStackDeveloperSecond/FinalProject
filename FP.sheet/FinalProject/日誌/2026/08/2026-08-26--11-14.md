---
文件類型: 開發日誌
建立時間: 2026-08-26 11:14
作者: alex／Codex
負責模組: QA-08
狀態: PR #48 已完成 Review、Required CI 並 Squash Merge 至 dev
相關分支: codex/qa08-authorization-isolation-evidence
相關 Commit/PR: 9e3ca96、4d68b15、1090ae3、04a6b0d／PR #48／Run 32929038131／Merge 660b724
---

# 開發日誌｜QA-08 現行 Endpoint 授權證據補強

## 作者與負責範圍

| 欄位 | 內容 |
|---|---|
| 作者 | alex／Codex |
| 負責模組 | QA-08 私人資源授權與拒絕零副作用 |
| 本次工作範圍 | Rebase 最新 dev；補後台商品全部現行寫入路由與退款折讓跨 Parent ID 的 SQL Server 證據；讓 Required CI 真正執行 SQL fixture；同步矩陣與追蹤 |
| 不在本次工作範圍 | 新增或改變產品行為、API、DTO、Schema、Migration、Package 或 SQL Server service |
| 第一線覆核／交接人 | alex |

## 更改內容

- 分支 rebase 至 `1cd671e`，納入退款折讓、中央 Audit、優惠券計算與最新 Migration。
- CatalogAdminAuthorizationTests 新增 10 個路由案例，逐一覆蓋品牌、分類、標籤、商品、SKU 的 POST／PUT。
- 每個商品寫入案例同時驗證匿名 401、OrderManager 403，並比較 Brands、Categories、Tags、Products、Skus、ProductTags 的 SQL 前後快照。
- InvoiceAllowanceWriterSqlServerTests 新增發票 B 搭配退款 A 的交叉 Parent ID 案例。
- 交叉折讓拒絕後不建立折讓表頭／明細、Audit 或 Idempotency，且原發票與交叉發票狀態／RowVersion 不變。
- CI Backend 設定 `DOSELECT_SQLSERVER_TEST_CONNECTION`；API／Infrastructure 固定資料庫 fixture 統一解析 CI 或本機連線，並保留各自專屬資料庫名稱，避免平行測試互刪。
- 更新 QA-08 覆蓋矩陣、測試策略及未完成項目追蹤表。
- PR #48 首次 Required CI Run `32927116408` 揭露三個 API fixture 將繼承的 `ConnectionStrings__DefaultConnection` 清為 `null`，使後續 Ubuntu 測試回退到 `.\SQL2025`；已改為保存並精確還原各環境變數原值。
- 同一 Idempotency-Key 的兩個 Merge 呼叫若未真正重疊，第二請求可合法 replay 已提交結果；測試現要求恰有一次非 replay 原始執行，另一請求可為 `request_in_progress` 或 replay，且資料只合併一次。

## 定義內容

| 取用者 | 必須遵守的規則 |
|---|---|
| Catalog 開發者 | 新增任何後台商品 POST／PUT／DELETE 路由時，同一 PR 增列匿名、錯誤角色與 SQL 快照證據 |
| 發票／退款開發者 | 路由 Invoice PublicId 與 Body Refund PublicId 是交叉 Parent ID 邊界；不同交易必須 404 且零資料副作用 |
| Reviewer | 僅驗證 401／403／404 不足；還要確認資料列、RowVersion、Audit、Idempotency、Outbox、通知及檔案不變 |
| CI 維護者 | Required CI 的 `dotnet test` 不得排除 `RequiresSqlServer`；必須提供 `DOSELECT_SQLSERVER_TEST_CONNECTION`，每個會建立／刪除資料庫的 fixture 必須使用專屬或隨機資料庫名稱 |
| 後續 Owner | 新增私人 Endpoint 時同步更新 QA-08 矩陣，不能把本次現行基線當成未來 Endpoint 的預先通過證據 |

## 驗證紀錄

| 驗證 | 結果 |
|---|---|
| SDK | 10.0.303 |
| 後台商品 focused | 10／10 通過；每案包含匿名與 OrderManager 兩次拒絕請求 |
| 退款折讓 focused | 1／1 通過；真實 SQL Server Migration Database |
| Build | 0 警告、0 錯誤 |
| Format | 通過 |
| PR #48 首次 Required CI | Run `32927116408`：Backend 失敗；確認為 fixture 清除共用連線與競態 replay 斷言兩項問題，不是產品授權或資料重複副作用 |
| PR #48 最終 Head Required CI | Run `32929038131`：Backend、雙 Frontend、Secret Scan、AI Evaluation Contract、Package Source Evidence 與 `CI Required` 全部通過；已 Squash Merge 為 `660b724` |
| 競態 focused 重跑 | 5／5 通過；每次恰有一次非 replay 執行，Cart 最終數量為 3 |
| API Integration | 使用獨立 `DoSelectApiSharedTests` 並套目前四支 Migration，303／303 通過、略過 0 |
| CI 等價完整 Solution | 設定兩個 SQL 連線環境變數後，Domain 314＋Application 202＋Infrastructure 317＋API Integration 303，共 1136／1136 通過、略過 0 |
| API／資料影響 | 無 API、DTO、Schema、Migration 或資料庫轉換；只新增測試、測試連線解析、CI 環境變數與證據文件 |

## 未完成

- [x] CI 修正 Commit `04a6b0d` 已推送。
- [x] PR #48 最終 Run `32929038131` 已取得 `CI Required` 成功證據。
- [x] 已完成獨立 Review 並 Squash Merge 至 `dev`（`660b724`）。
- [ ] 未來新增私人 Endpoint 時持續更新矩陣。
