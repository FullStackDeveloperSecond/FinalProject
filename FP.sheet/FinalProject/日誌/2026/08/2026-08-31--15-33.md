# 2026-08-31 15:33｜模擬付款差異與 COD 交付邊界

- 作者：alex／Codex
- 更改內容：
  - 記錄 DEC-P347：不新增平行履約 Endpoint；模擬付款完成端點拒絕 COD，先交付 Delivered／PickedUp 決策服務。
  - `codex/payment-completion-alignment` 已實作 `cancelled`、付款成功模擬發票 Outbox／Consumer、OpenAPI 與 Typed Client；Application 測試與 Solution Build 通過。
  - SQL Provider 測試在受限身分下因 SSPI 失敗；經組長授權改用系統身分與隨機測試資料庫後，首次執行找出 `invoice.simulated.requested.v1` 未加入 Infrastructure serializer registry，補齊登錄與回歸測試後，付款 SQL Provider 類別 23／23 通過，隨機資料庫均由測試清除。
  - 目前證據為本機工作分支驗證；尚未完成 Commit、Review 或合併，不因此宣告 M-09、DES-22 或 COD 垂直流程完成。
- 定義的內容：
  - `simulationKey`：模擬付款完成操作的唯一重播鍵，不增加第二個 Header。
  - `CashOnDeliveryCompletionService`：只定義 COD 在 `Delivered`／`PickedUp` 可收款的 Application 決策；不擁有物流 Endpoint。
  - `invoice.simulated.requested.v1`：付款成功交易內寫入的 Outbox 事件；Consumer 以一張訂單一張模擬發票的唯一性冪等建立發票。
  - 未來物流 writer 若要取用 COD 規則，必須在正式履約狀態命令的同一交易邊界套用付款計畫並寫入通知、Audit 與發票 Outbox；完成前不得宣告 COD 垂直流程完成。
