# 2026-08-21 16:15 客服模組 PR 準備與流程調整日誌

## 完成內容

- 將客服前後台 UI、客服案件核心、SLA 工作台、安全附件上傳與下載等七個提交 Rebase 至最新 `origin/dev`。
- Rebase 衝突保留組長新增的退款基礎設施註冊與客服模組註冊，未覆蓋 dev 更新。
- 更新遠端 `feature/support-tickets`，本機與遠端個人分支一致。
- 準備合併至 `dev` 的 PR 標題、說明、驗證方式與已知環境限制，交由本人於 GitHub 送出。
- 調整 development-pipeline skill：完成作業優先、一次必要驗證、僅在方向疑慮時以選擇題詢問，高風險或明確要求時才使用獨立測試代理。

## Rebase 與分支狀態

- 目前分支：`feature/support-tickets`
- 最新提交：`424ef3c feat(support): add customer and admin case UI`
- 最新 dev 基底：`319b342`
- 相對 `origin/dev`：領先 7、落後 0。
- 相對 `origin/feature/support-tickets`：同步完成。

## Rebase 後驗證

- Customer：30/30；typecheck、lint、production build 通過。
- Admin：16/16；typecheck、production build 通過；lint 只有既有測試檔 2 項 warning，無 error。
- Backend Domain：180/180。
- Backend Application：114/114。
- Backend Infrastructure：173/173。
- API Integration：93/132；39 項均受本機 SQL Server 加密相容性問題阻擋。
- `git diff --check` 通過。

## 已知限制

- 本機 SQL Client 回報「SQL Server 執行個體需要加密，但此電腦不支援」，因此依賴真實 SQL Server 的 39 項整合驗收尚未在本機建立通過證據。
- 此次未停用加密、未修改連線安全設定，也未將環境錯誤誤判為正式程式缺陷。

## 如何確認

```powershell
git fetch origin
git switch feature/support-tickets
git status
git rev-list --left-right --count HEAD...origin/feature/support-tickets
git rev-list --left-right --count HEAD...origin/dev
git log --oneline --decorate -8
```

預期個人分支顯示 `0 0`；相對 dev 顯示本分支提交數與 `0`，表示沒有落後 dev。
