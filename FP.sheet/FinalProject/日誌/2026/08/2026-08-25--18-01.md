---
文件類型: 開發日誌
建立時間: 2026-08-25 18:01
作者: alex／Codex
負責模組: QA-08
狀態: 第一批 Actor A／B 自動化矩陣、購物車零副作用修正與本機完整驗證完成；PR 待完成
相關分支: codex/qa08-authorization-isolation-evidence
相關 Commit/PR: f66a2d9＋本次修正待提交／PR 待建立
---

# 開發日誌｜QA-08 Actor A／B 與刪除無副作用證據

## 作者與負責範圍

| 欄位 | 內容 |
|---|---|
| 作者 | alex／Codex |
| 負責模組 | QA-08 私人資源授權、寫入／刪除無副作用 |
| 本次工作範圍 | 盤點目前 Endpoint；補購物車、客服案件／附件與 SKU 刪除整合案例；建立覆蓋矩陣；沿用現有 SQL Server service 將 Provider-backed 測試納入 Required CI |
| 不在本次工作範圍 | 未經裁定修改正式購物車行為、Schema／Migration、新增 Endpoint 或新增 CI 基礎設施 |
| 第一線覆核／交接人 | alex |

## 更改內容

- 新增訪客購物車 Actor A／B 列表、修改、刪除三案。
- 新增會員購物車 Actor A／B 列表、修改、刪除三案。
- 新增客服列表隔離、跨會員新增訊息與取消三案。
- 新增跨會員附件上傳一案，驗證 404、資料庫零附件及 private 暫存／最終目錄無檔案。
- 新增 SKU 匿名與錯誤角色刪除兩案，驗證 SKU 與 RowVersion 不變。
- 建立 [[03-架構/08-測試與驗收/QA-08私人資源授權覆蓋矩陣]]，明列已覆蓋、不適用與尚待完成項目。
- PR #41 已由 Run `32851496154` 完整通過並 Squash Merge 至 `dev`（`64b0ef4`）。
- 依 DEC-P307，Backend Job 沿用已存在的 SQL Server 2025 service、Migration 與連線字串，移除 `Category!=RequiresSqlServer` filter；不新增 Runner、服務或套件。
- 依 DEC-P308，購物車修改／刪除改為只查既有、Active 且未逾期的呼叫者 Cart；不存在或逾期沿用 `404 resource_not_found`，拒絕時不建立 Actor B 空購物車。
- 補上呼叫者既有 Cart 已逾期的 provider-backed 回歸案例，證明拒絕修改不會更新 Cart 狀態／到期時間／UpdatedAt／RowVersion、Item 或總筆數。

## 定義與跨模組取用

| 取用者 | 必須遵守的規則 |
|---|---|
| API 開發者 | 新增私人讀取、寫入、刪除、附件、匯出、批次或背景操作時，同一 PR 更新矩陣並補 Actor A／B 案例 |
| Reviewer | 拒絕狀態碼之外，還要查核資料列、RowVersion、歷程、Outbox／通知及檔案等副作用 |
| QA／CI 維護者 | Required CI 必須維持 SQL Server service 與無過濾 `dotnet test`；遠端 Run 成功前只可記錄為已設定，不可誤寫為已驗證 |
| 購物車開發者 | Guest Cart Key 是訪客購物車持有憑證；訪客 A／B 隔離以不同 Key 驗證 |
| 後台開發者 | 單一商家 SKU 沒有 Merchant Scope；以 CatalogManager Policy、業務狀態與 RowVersion 驗證，不虛構跨商家案例 |

## 驗證紀錄

| 驗證 | 結果 |
|---|---|
| Integration Test Project Build | 0 警告、0 錯誤 |
| 原始四個整合測試類別 | 56／56 通過 |
| 本次修正後完整 Solution | SDK 10.0.303；Build 0 警告／0 錯誤、Format 通過；Domain 194＋Application 177＋Infrastructure 262＋API Integration 279，共 912／912 通過 |
| 新增案例 | 13 案（原始 Actor／角色整合案例 12 案＋過期 Cart provider-backed 案例 1 案） |
| SQL Server | 使用既有專用測試資料庫與正式 EF Core Model |
| 實體檔案副作用 | 跨會員附件上傳後 private 根目錄零檔案 |
| Package 來源證據 | PR #41 Run `32851496154` 完整通過並已 Squash Merge，Merge Commit `64b0ef4` |
| CI 靜態查核 | Backend 已有 SQL Server 2025 service、連線字串及 Migration；本分支移除 `Category!=RequiresSqlServer` filter，遠端證據待 PR |
| 修正前紅燈 | SDK 10.0.400 的首次重現 filter 執行 6 案：列表 2 案通過，修改／刪除 4 案因 Cart 筆數 N→N+1 失敗；正式綠燈改在 `FP.dev` 使用鎖定 SDK |
| DEC-P308 綠燈 | `FP.dev` 解析 SDK 10.0.303；相同 Actor B filter 6／6 通過，四個寫入案例證明 404、Actor A 數量／RowVersion／資料列不變且 Cart 總筆數不增加 |
| 過期 Cart 綠燈 | CartService focused 1／1 通過；拒絕後 Cart／Item 快照與 Cart 總筆數完全不變 |
| 變更邊界 | 修改既有 EF Cart 查詢路徑、測試、文件與 CI 測試命令；未改 API、Schema、Migration、套件或新增基礎設施 |

## 未完成

- [x] PR #41 已完成 Review、Required CI 與 Squash Merge，Package 來源證據已進入 `dev`。
- [ ] 取得移除 SQL 測試 filter 後的 Required CI 成功證據。
- [x] DEC-P308 已定版並完成 Actor B 空購物車與過期 Cart 拒絕寫入副作用修正；Actor B focused SQL Server filter 6／6、過期 Cart focused 1／1 通過。
- [ ] 各 Owner 新增訂單、退貨、退款、收藏、通知、匯出等私人 Endpoint 時持續補矩陣。
- [ ] 本分支重新驗證、Commit／Push 並建立 PR。
