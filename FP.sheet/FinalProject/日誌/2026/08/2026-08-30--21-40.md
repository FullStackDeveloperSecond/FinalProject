---
文件類型: 開發日誌
建立時間: 2026-08-30 21:40
作者: yinyin
負責模組: M-07 優惠券（A-23 review 第二輪）
狀態: 待覆核
相關分支: feature/admin-coupon-page
相關 Commit/PR: PR #64
---

# 開發日誌

## 作者與負責範圍

| 欄位 | 內容 |
|---|---|
| 作者 | yinyin |
| 負責模組 | M-07（A-23 後台優惠券頁面） |
| 本次工作範圍 | alex 第二輪 review 的 P2#1、P2#2、P3#4、P3#5 |
| 不在本次工作範圍 | P2#3 批次目錄參考端點（後端、Catalog 模組，歸屬待裁定） |
| 第一線覆核／交接人 | haru |
| 狀態 | 待覆核 |

## alex 回答了目錄參考資料的權限問題

我上一輪問「要不要放寬 `CatalogManager`」，他的裁定是：

> **不要放寬** —— 那會把 FinanceManager／MarketingAnalyst 的權限擴張到目錄管理邊界。
> 建立受 `Coupon.Manage` 保護、用途限定的**批次**目錄參考端點。

他同時指出我那套公開端點方案的實際成本，而且說得對：分類樹每個節點各打一次
`/catalog/filter-options`（上限 100 次），而那個端點每次還會順便算品牌、價格區間、
規格篩選；`resolveProductOptions` 又對每個已選商品逐筆查明細，兩個 picker 各最多 50 次。

**我當時只想到「哪個端點權限過得去」，沒有算過它會放大成多少次查詢。**
選一個公開端點來繞過權限問題，代價是把一次編輯變成上百次 HTTP 與 SQL。

這一項是後端、而且落在 Catalog 模組，已在 PR 問他歸屬（我做／指派 Catalog owner／他自己接），
連同端點路徑與「能不能選草稿／停用項目」一起確認。

## P2#2：測試又一次證明不了它看起來在證明的事

百分比券後端要求 `maximumDiscount > 0`，前端沒把它設成必填。管理員照介面正常填完，
按下儲存會直接吃一句英文 400。

而**現有那條百分比測試是綠的** —— 因為它 mock 掉 API，套不到後端的 `RequireValidRule`。
它送出的正是一組真實 API 會拒絕的輸入。

補上前端規則後，那條測試第一次跑就變紅了。這是它應該有的行為 —— 我把它改成填上最高折抵，
並補了「未填會擋下且不呼叫 API」與「填了會帶進 payload」兩條。

**要記的是**：mock 掉 API 的元件測試，證明的是「前端送了什麼」，不是「後端會不會收」。
凡是有後端驗證規則的欄位，前端測試至少要有一條對著那條規則寫。

## P2#1：停用要先確認

`disabled` 是終態，誤觸後沒有任何方式重新啟用。加確認對話框，顯示優惠碼、名稱、
目前狀態、使用量、停用影響與不可逆性，並明講「需要暫時停售請改用暫停」。

只對 `disable` 擋一層：`activate` 與 `pause` 都可以再切回來。

測試三條：出現對話框且**不呼叫 API**、取消後仍**不呼叫 API**、確認後才送出。
中間那條是重點 —— 對話框存在的理由就是取消時什麼都不做。

## P3#4／P3#5

側欄的優惠券入口改成依角色顯示（與 router 的 `requiredRoles` 同一份清單）。
只動新加入的那一個入口，不順便重構整個側欄 —— alex 明講不必。

列表條件（q／statuses／pageNumber）同步進 Router Query，照 `ProductsPage.vue` 的既有寫法。
兩個細節：

- 用 `replace` 不用 `push`，否則每打一個字就堆一筆瀏覽記錄，返回鍵變成一次退一個字元
- 還原的順序有意義：先填 `q` 再 `search()`（立即套用但會把頁碼重設為 1），最後才寫回頁碼。
  反過來的話還原出來永遠是第 1 頁

## 破壞式驗證

五個修正逐一拿掉跑一次，確認**恰好**是預期的那幾條變紅：

| 拿掉的修正 | 變紅 |
|---|---|
| 百分比不再要求最高折抵 | 2（單元 1、頁面 1） |
| 停用不再先確認 | 2 |
| 側欄不再依角色 | 2 |
| 不再從網址還原條件 | 2 |
| 不再把條件寫回網址 | 1 |

還原後 46 條全綠。

## 驗證

| 項目 | 結果 |
|---|---|
| `vue-tsc` typecheck | 通過 |
| ESLint（`--max-warnings 0`） | 通過 |
| admin-web 單元測試 | 227／227（由 206 增加 21） |
| `npm run build` | 通過 |
| `npm audit --omit=dev` | 0 vulnerabilities |

## 待處理

- P2#3 批次目錄參考端點：歸屬、路徑與「草稿／停用項目可否選取」待 alex 裁定

---

## 2026-08-31 追記：改用 #69 的批次端點

#69 合併後，把挑選器的資料來源從公開端點換成
`/api/v1/admin/coupons/catalog-options`。

| | 之前 | 現在 |
|---|---|---|
| 分類 | 每個樹節點各打一次（上限 100） | **一次** |
| 已選商品 | 逐筆查明細（兩個 picker 各最多 50） | **一次**批次解析 |
| 型別 | 手寫平行型別 | 產生的 typed client |

手寫平行型別當初是因為打的是店面端點、形狀跟 picker 要的不一樣；
現在後端回的就是 picker 需要的欄位，那份手寫的型別沒有存在理由了。

### 順帶修掉一個自己造的「沒有出口的狀態」

舊版的搜尋結果多於一頁時顯示「只顯示前 10 筆，請輸入更精確的關鍵字」——
這跟 alex 在 #69 P2 點的是**同一個毛病**：介面說「還有更多」，卻沒有任何方法拿到那些更多的東西。

端點修好之後這裡也要跟上，改成真的上一頁／下一頁。換關鍵字時回到第一頁——
否則會拿新關鍵字去查舊頁碼。

### 停售商品現在看得出來

搜尋結果與已選清單都顯示狀態；停售品的 checkbox 是 `disabled`（搜尋結果本來就不會回它們，
這是防呆），已選清單則在名稱後標「（已停售）」——它仍然生效，但管理員多半會想改掉。

**測試因此紅了一條**：舊 mock 沒有 `isSelectable`，`!undefined` 是 `true` 所以 checkbox 變成
disabled、勾不動。抽了一個 `productOption()` 工廠，形狀跟真的一致。

### rebase 衝突

`errorMessages.ts` 與 `App.vue` 各一個，都是 `dev` 上新加的項目（相容性規則）與本支新加的項目
（優惠券）落在同一個位置。**兩邊都保留**，不是二選一。

### 驗證

| 項目 | 結果 |
|---|---|
| `vue-tsc` typecheck | 通過 |
| ESLint（`--max-warnings 0`） | 通過 |
| admin-web 單元測試 | 270／270 |
| `npm run build` | 通過 |
| `npm audit --omit=dev` | 0 vulnerabilities |
| 基底 | 已 rebase 至 `dev@148e8c6`（#69 合併後） |
