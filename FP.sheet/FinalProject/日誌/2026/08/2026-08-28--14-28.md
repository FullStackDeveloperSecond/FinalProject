---
文件類型: 開發日誌
建立時間: 2026-08-28 14:28
作者: alex／Codex
負責模組: AI-13
狀態: 待覆核
相關分支: codex/ai13-completion
相關 Commit/PR: PR #57
---

# 開發日誌｜AI-13 PR Review 修正

## 作者與負責範圍

| 欄位 | 內容 |
|---|---|
| 作者 | alex／Codex |
| 負責模組 | AI-13 同意政策、每日額度與 Migration Gate |
| 本次工作範圍 | 修正目前同意版本、Support 用途、台灣午夜額度與 PR #57 Migration |
| 不在本次工作範圍 | OpenAI Responses API Adapter、M-19、M-18、Live Model 評估 |

## 更改內容

- 新增 `AiConsentPolicy.CurrentVersion = 1` 與 `AiConsentPurpose.Support`。
- Admission Gate 只讀目前版本與 Support 用途的最新 append-only 同意；其他版本 Fail Closed 為尚未同意。
- 每日額度由錯誤的 UTC 00:00 改為 `Asia/Taipei` 00:00，資料仍以 UTC 查詢。
- `AiConsentRecords` 增加 `Purpose varchar(16)`、Check Constraint 與 `MemberUserId＋Purpose＋PolicyVersion＋CreatedAtUtc` 複合索引。
- 寫回 DEC-BATCH-032／DEC-P328～P330，修正 DEC-BATCH-031、AI 規格、資料字典、威脅模型及追蹤表。

## Migration Gate

重建 Migration 時，EF 因前一支 `AddCompatibilityCheckRunsEvaluatedAtUtcIndex.Designer.cs` 缺少更早已存在的 Product Image Hash metadata，而錯誤嘗試在 AI Migration 重複新增三個 Hash 欄位。依 alex 核准的 A 方案，恢復原 `20260828050333_AddAiSafetyConsentAndUsage` 並只修改 AI 新表、Designer 與 Snapshot；前一支已合併 Migration 不動。

最終 `Up()` 只新增 `AiConsentRecords`、`AiUsageLedger` 及其索引、Constraint、FK，沒有既有表 Add／Alter／Drop。共用 `DoSelectDb` 未更新。

## 測試與驗證

| 驗證 | 結果 |
|---|---|
| 修正前回歸 | 版本不符錯誤回 `Granted`；台灣午夜前額度錯誤剩餘 20，兩項均按預期失敗 |
| AI focused | Domain 4、Application 32、Infrastructure 8、API 10，全數通過 |
| 完整後端 | Domain 447、Application 363、Infrastructure 506、API 479，共 1,795／1,795 通過 |
| Migration | `has-pending-model-changes` 無差異；獨立可拋棄 DB 完成 18 支 Migration 重播 |
| API 隔離驗證 | 479／479；驗證 DB 已刪除 |
| Build | 0 warning／0 error |

## 定義與交接契約

| 取用者 | 契約 |
|---|---|
| M-19 同意 Endpoint／UI | 只可提交目前政策版本與 `Purpose=Support`；版本不符必須拒絕，不能由前端決定有效版本 |
| OpenAI Adapter | Admission 未通過不得呼叫外部模型；不得自行忽略版本、用途或額度結果 |
| 政策維護者 | 政策文字升版需與 `AiConsentPolicy.CurrentVersion`、同意 UI、測試與部署同批修改 |
| 報表／額度 | 每日歸屬以 `Asia/Taipei` 00:00 切日，資料庫時間仍保存 UTC |

## 後續工作

- [ ] Commit／Push PR #57 修正。
- [ ] Required CI 通過後以遠端 Head SHA 重新唯讀 Review。
- [ ] Review 無問題後 Squash Merge 並刪除分支。
- [ ] 合併後開始 OpenAI Responses API Adapter。

## 相關連結

- [[05-規劃/03-需求與決策治理/決策/02-已寫回/DEC-BATCH-032-AI客服同意版本用途與額度日界線修正]]
- [[03-架構/06-AI設計/AI應用詳細設計]]
- [[03-架構/06-AI設計/AI測試與評估規格]]
- [[05-規劃/01-時程與進度/未完成項目追蹤表]]
