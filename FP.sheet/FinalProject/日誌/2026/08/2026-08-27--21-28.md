---
作者: alex／Codex
日期: 2026-08-27 21:28
分支: haru/feature/guest-cancelentry
PR: 43
狀態: dev 已合入，修正、完整本機驗證與最終 review 完成，待 Commit／Push／CI
---

# 2026-08-27 21:28｜PR #43 訂單取消整合修正

## 授權與裁定

- alex 授權將最新 `dev@fddcb18c` merge 進 PR #43 分支。
- alex 採用 A1：待付款取消的訂單、庫存、優惠券、歷程與中央 Audit 必須同交易原子完成，失敗整體回滾。
- alex 採用 B1：取消原因固定為 `changed_mind`、`ordered_by_mistake`、`found_better_price`、`shipping_too_slow`、`other`。
- alex 授權以管理員權限繞過 Review Gate，squash merge PR #53，並將更新後的 dev 合入 PR #43。
- 未授權發布 GitHub Comment／Review、Approve 或 Merge。

## 修正內容

- 解決 `Program.cs` 與 customer router 的 dev merge 衝突，保留 dev 所有註冊與路由後加入 Orders 能力。
- 補齊庫存保留釋放、餘額還原、反向 Movement、優惠券兌換釋放／狀態恢復、OrderStatusHistory 與中央 Audit。
- 統一取消原因碼與 API 實際 camelCase enum 契約。
- 退貨期限改用既有臺灣時區冷靜期政策，訂單明細頁接上既有退貨 Route。
- 新增 SQL Server Provider-backed 與 API Integration 回歸測試；未建立或套用 Migration，未寫入共用 `DoSelectDb`。
- 產生 OpenAPI 與 TypeScript Schema；開發 API 使用隔離連線字串，背景清理查詢因資料庫不存在而失敗，沒有建立或修改資料。
- dev CI hotfix 以獨立 PR #53 修復 Return DTO metadata、歷史 Migration 測試 seed、Migration BOM 與 OpenAPI artifact；Required CI 全綠後由 alex 授權管理員 squash merge。
- PR #43 已先合入 `dev@fddcb18c`，再合入 PR #53 後的 `dev@8fb7a6f`；第二次同步無衝突。

## 已完成驗證

- `dotnet restore DoSelect.slnx`：通過。
- `dotnet build DoSelect.slnx --no-restore`：0 warning、0 error。
- Orders SQL Server 測試：7／7 通過。
- Orders API Integration：2／2 通過。
- OrderDetailPage focused Vitest：6／6 通過。
- 完整 .NET 測試：1,486／1,486 通過（Domain 424、Application 307、Infrastructure 393、API Integration 362）。
- customer-web 完整 Vitest：19 files、76／76 通過。
- customer-web `typecheck`、`lint`、`build`：通過。
- `npm run api:check`：通過；OpenAPI 與 TypeScript Schema 無內容差異。
- `dotnet format DoSelect.slnx --no-restore --verify-no-changes`：通過。
- `npm ci`：完成，0 vulnerabilities；保留既有 `glob@10.5.0` 提示。
- `git diff --check`：無 whitespace error，只有 Windows LF／CRLF 提示。
- 依 `dev@8fb7a6f` 基準完成整體 diff review，未發現新的阻擋問題。

## 待完成

- Commit、Push 後等待 Required CI，並以遠端 Head SHA 再做唯讀 review。
- 若達到可合併狀態，另向 alex 取得 Approve／Squash Merge 授權。

## 2026-08-28｜PR #40 合併後續修復迭代

### 授權與範圍

- 依 alex 先前「將最新 dev merge 進 PR #43」與本輪「繼續處理 #43」授權，在 PR #43 專用 worktree 合入 `dev@db7b17f`；本機 merge commit 為 `ee13c6c`，尚未 Push 時開始本輪 review。
- 變更限於 PR #40 落地後的 guest 訂單整合與被新必要設定打壞的既有測試 fixture；不新增 schema、Migration、套件或第二套授權流程。
- 未授權發布 GitHub Comment／Review、Approve 或 Merge。

### Review 發現與最低成本裁定

- P1：OrdersController 仍為 class-level Member policy，PR #40 核發的 GuestOrderAccess Cookie 無法存取 PR #43 的訂單明細／取消端點，M-02 guest 流程未完成。
- P1：ReturnsApiFixture 未提供 `GuestOrderAccess:Pepper`，實跑 OrdersMemberHttpTests 2／2 在 API 啟動時失敗；fixture 初始化失敗後 Dispose 又因未初始化 factory 產生 NullReferenceException。
- 最小充分方案：重用既有 `GuestOrderAccessScopeAuthorizer` 與同一個 EfOrderService 取消交易；會員清單維持 Member policy，只有單筆明細與取消接受已限單驗證的 guest。未採用文件／設定替代，因其無法完成 server-side 資源授權與取消行為。

### 修正與測試迭代

- 先新增 guest 自有訂單明細／取消與跨訂單拒絕兩個 API 回歸測試；修正前實跑 0／2 通過，兩案均因 OrdersController 回 401 失敗，符合預期紅燈原因。
- GuestOrderAccessScopeAuthorizer 成功結果補帶 token PublicId，供取消中央 Audit 使用；Cookie 仍不含訂單識別碼，限單比對仍逐次查 DB。
- OrdersController 採會員優先的手動 actor resolution：列表仍為 Member policy；單筆明細／取消先驗 Member，再以中央 authorizer 驗 Guest，無身分回 401、已驗證 guest 跨單回 404 並保留 scope violation audit。
- EfOrderService 以受信任的 Member／Guest actor 共用同一套訂單讀取與原子取消流程；guest 僅能查 guest-owned order，歷程與 InventoryMovement 的 ActorUserId 為 null，中央 Audit 使用 Guest token PublicId。
- ReturnsApiFixture 補測試 pepper，並讓初始化失敗後的清理可安全執行；所有 SQL 測試均使用各自隨機命名資料庫，未讀寫共用 `DoSelectDb`。
- 第一輪修正後：solution build 0 warning／0 error；會員 API 2／2、Orders service 7／7、guest 跨單測試通過。guest 自有訂單測試因測試端預設 enum converter 無法解析 API camelCase enum 而失敗；改以公開 JSON contract 讀取 RowVersion 後重跑 guest API 2／2 通過，未更改產品 enum 行為。

### 本輪仍待完成

- `dotnet build DoSelect.slnx --no-restore -warnaserror`：0 warning、0 error。
- PR #43 相關隔離 SQL 證據：guest Orders API 2／2、member Orders API 2／2、EfOrderService 7／7 通過。
- customer-web：typecheck、lint、production build 通過；完整 Vitest 19 files、76／76 通過。
- `dotnet format DoSelect.slnx --no-restore --verify-no-changes`：通過。
- `npm run api:check`：第一次僅因 exporter 預設 `localhost` 與 repo artifact 的 `127.0.0.1` 不同而失敗；指定相同 base URL 重跑通過，OpenAPI／TypeScript schema 無內容 drift。Contract API 指向不存在的專屬 DB，未建庫、未套 migration；背景 cleanup 查詢因 DB 不存在失敗，沒有寫入任何 DB。
- 完整 `dotnet test DoSelect.slnx` 第一次：Domain／Application／Infrastructure 通過；API Integration 因 shell 未提供 CI 的 GuestOrderAccess pepper 而 314 例在 host 啟動前失敗。嘗試依 CI 補 pepper 重跑時，安全檢查判定仍可能沿用 appsettings 的共用 `DoSelectDb` 而阻擋；依 alex 隔離規則未繞過，完整 API matrix 留給 Required CI 的一次性 SQL Server。
- `git diff --check origin/dev`：無 whitespace error，只有 Windows LF／CRLF 提示。
- 依 `origin/dev@db7b17f` 對本機最終內容完成授權、跨單、audit、交易、API 相容與測試隔離 review，未發現新的 P0～P2 finding。

### 下一步

- 建立普通 commit、Push、等待 Required CI，再依遠端 Head SHA 做唯讀 review/test。
- 若遠端達到可合併狀態，另向 alex 取得 GitHub Approve／Squash Merge 授權；目前仍未授權發布 GitHub Comment／Review、Approve 或 Merge。

### CI 修復迭代

- Push `12cec58` 後 CI run `33097206293`：Frontend admin/customer、Package Source、AI Evaluation、Secret Scan、Browser E2E 全部通過；Backend restore、build、format、migration 通過。
- Backend 在 OpenAPI currentness 唯一失敗：PR artifact 的 server URL 為 `http://127.0.0.1:5126/`，CI exporter 依既有 script 預設產生 `http://localhost:5126/`；因此未繼續執行該 job 的完整 .NET tests。
- `origin/dev@db7b17f` 的正式 artifact 同樣是 `http://localhost:5126/`。採最小修正只恢復該生成物 URL，不改 exporter、CI、route 或 DTO；TypeScript schema 沒有差異。
