---
文件類型: 開發日誌
建立時間: 2026-08-28 01:33
作者: Terry910203000
負責模組: M-16／M-17 自由組裝與相容性（feature/build-compat-api, PR #34）
狀態: 待覆核
相關分支: feature/build-compat-api
相關 Commit/PR: HEAD `26c2ac5`（此次改動尚未提交）／PR #34
---

# 開發日誌

## 作者與負責範圍

| 欄位 | 內容 |
|---|---|
| 作者 | Terry910203000 |
| 負責模組 | M-16 自由組裝清單、M-17 零件相容性（PR #34 `feature/build-compat-api`） |
| 本次工作範圍 | 依 DEC-BATCH-027 與組長 PR #34 round-7 review，刪除本 PR 自建的平行相容性資料模型，改用正式的 `ICompatibilityCatalogReader`／`CompatibilityEvaluator`；連帶處理遺留的 `20260826060901_AddSkuStorageInterfacePorts` Migration 該如何清理，向 alex 提出這支 Migration 需要走 Migration Gate |
| 不在本次工作範圍 | 實際 scaffold「刪除 SkuCompatibilityAttributes／SkuStorageInterfacePorts 兩張表」的新 Migration（依 Migration Gate 規則不可自行動手，見下方說明） |
| 第一線覆核／交接人 | alex（Migration Gate）；kafen（Terry 模組主要覆核） |
| 開始時間 | 2026-08-27 |
| 完成時間 | 2026-08-28 01:33（模型收斂與測試改寫已完成並通過建置；Migration 清理待 alex Gate 覆核，非最終完成） |
| 狀態 | 待覆核 |

## 更改內容

### 摘要

組長在 PR #34 的 review 中引用 DEC-BATCH-027（相容性來源多值規格與 Checkout 原子交易定版），裁定本 PR 自建的 `SkuCompatibilityAttributes`／`SkuStorageInterfacePorts`／`CompatibilityRuleEngine` 一整套平行模型不再是正式來源，改以 Catalog 模組既有的 `ICompatibilityCatalogReader`／`EfCompatibilityCatalogReader`（建構於 `SpecificationDefinition.AllowsMultiple`＋`SkuSpecificationOptionSelection` 正規化多值 join 表）與 Domain.Builds 的 `CompatibilityEvaluator`（PR #52 既有產物）為正式相容性事實來源與規則引擎。已依此完成：

- 刪除本 PR 自建的整套平行模型（entity、admin service、DTO、規則引擎），並將 `EfCompatibilityCheckService`／`EfCompatibilityRuleAdminService`／`EfBuildListService` 全部改接正式的 `ICompatibilityCatalogReader`＋`CompatibilityEvaluator`。
- 改寫全部 13 個受影響測試檔（`EfBuildListServiceTests`、`CompatibilityCheckServiceTests`、`EfCompatibilityRuleAdminServiceTests` 等），改用正式多值模型的 seeding 方式；刪除兩個已無對應正式邏輯可測的測試檔（`EfSkuCompatibilityAttributeAdminServiceTests`、`ProductCategoryChangeCompatibilityGuardTests`，後者的 scalar/decimal 值涵蓋已由 `CatalogAdminServiceTests` 既有測試證實，另補一支涵蓋多值 `SkuSpecificationOptionSelections` 路徑的測試）。
- 整個 solution 重新建置為 0 錯誤、0 警告；已用本機啟動的後端重新產生 `contracts/openapi.v1.json` 與前端 `schema.d.ts`，差異剛好等於被移除的兩個 SKU-attributes 端點。

**Migration 清理未完成，需要 alex 的 Migration Gate 覆核才能繼續：** `20260826060901_AddSkuStorageInterfacePorts`（先前已在 [[2026-08-26--20-28]] 提出待覆核）在這次 rebase／後續 dev 演進後，已不再是 Migration 歷史中的最後一支（`dev` 上又疊了多支後續 Migration），`dotnet ef migrations remove` 因此無法直接刪除它——唯一乾淨的清理方式是另外新增一支「drop `SkuCompatibilityAttributes`／`SkuStorageInterfacePorts` 兩張表」的新 Migration。依最新工程包規則（「不得在功能分支自行 scaffold／apply 新 Migration；Schema 需求交 alex 走 Migration Gate」），本次**未**自行 scaffold 這支新 Migration，也**未**手動修改 `DoSelectDbContextModelSnapshot.cs` 讓 `has-pending-model-changes` 看起來乾淨（那樣只是掩蓋、不是解決：目前 Migration 歷史重播出來的 DB 仍會建出這兩張表，但正式 C# Model 已經沒有對應 Entity，兩者不一致）。目前 `dotnet ef migrations has-pending-model-changes` 會正確回報有 pending changes，這是預期中、如實反映問題的狀態，會維持到 alex 排出 drop migration 為止。

### 新增

- 無新增 Migration（本次刻意不 scaffold，見上方說明）。

### 修改

- 無（`DoSelectDbContextModelSnapshot.cs` 維持原樣，未手動調整）。

### 刪除或淘汰

- 無（`20260826060901_AddSkuStorageInterfacePorts.cs`／`.Designer.cs` 維持原樣保留在 Migration 歷史中，等待 alex 排出對應的 drop migration 後才能真正處理）。

## 定義與對外契約

### 提供給其他模組的能力或資料

| 能力／資料 | 提供者 | 使用者 | 用途 |
|---|---|---|---|
| 不適用 | 不適用 | 不適用 | 本次為刪除本模組自建的平行來源，改用既有正式來源，未新增對外契約 |

### 正確取用方式

| 項目 | 定義 |
|---|---|
| 前端 API 路由與 HTTP Method | `DELETE`：`GET`／`PUT /api/v1/admin/compatibility-rules/skus/{skuPublicId}/attributes` 已隨平行模型一併移除，改由既有 Catalog 商品／SKU 規格管理端點承接相容性事實的寫入 |
| Request DTO | 不適用 |
| Response DTO | 不適用 |
| 後端介面／Application Service | `ICompatibilityCatalogReader`／`EfCompatibilityCatalogReader`（Catalog 模組既有，非本次新增）、`CompatibilityEvaluator`（Domain.Builds，PR #52 既有） |
| 領域事件／整合事件 | 無 |
| 必要權限或 Policy | 不適用 |
| 成功條件 | 不適用 |
| 可能錯誤碼 | 不適用 |

### 命名與型別

不適用（本次未新增欄位／型別，僅刪除本 PR 自建、已被正式來源取代的模型）。

### 必須維持的規則

- 相容性事實的唯一正式來源自此為 `ICompatibilityCatalogReader`＋`CompatibilityEvaluator`；不得再於任何功能分支重新自建平行模型。

### 禁止或不建議的取用方式

- 不可由前端直接存取資料庫。
- 不可跨模組直接操作其他模組的 Repository 或資料表。
- 其他：**不得在其他功能分支自行 scaffold 處理 `SkuCompatibilityAttributes`／`SkuStorageInterfacePorts` 清理的 Migration**——待 alex Migration Gate 覆核後統一處理，避免多支分支各自排出衝突的 drop migration。

## 資料庫影響

| 項目 | 內容 |
|---|---|
| 新增／修改資料表 | 無（本次未新增／修改任何資料表結構） |
| 新增／修改欄位 | 無 |
| 索引與唯一性約束 | 無異動 |
| 外鍵與刪除行為 | 無異動 |
| Migration | **`20260826060901_AddSkuStorageInterfacePorts` 待清理**——需要 alex 排出對應的 drop migration（刪除 `SkuCompatibilityAttributes`／`SkuStorageInterfacePorts` 兩張表），本次未自行 scaffold |
| Seed／既有資料處理 | `MinimalDevelopmentDataSeeder` 已改用正式多值模型 seeding（`SpecificationOption`＋`SkuSpecificationValue`／`SkuSpecificationOptionSelection`），本機重跑 `--seed-minimal` 後不再寫入 `SkuCompatibilityAttributes`／`SkuStorageInterfacePorts` |
| 正規化／反正規化考量 | 不適用 |

## API、事件與畫面影響

| 類型 | 路由／名稱／頁面 | 影響與相容性 |
|---|---|---|
| API | `GET`／`PUT /api/v1/admin/compatibility-rules/skus/{skuPublicId}/attributes` | 移除（本 PR 自建、從未正式對外發布過的端點，改由既有 Catalog SKU 規格管理端點承接） |
| 事件 | 無 | 無 |
| 前台畫面 | 無 | 無（本 PR 相關前台畫面尚未開發） |
| 後台畫面 | 無 | 無（本 PR 相關後台畫面尚未開發） |

## 設定與執行方式

| 項目 | 內容 |
|---|---|
| 新增環境變數 | 無 |
| 新增套件／外部服務 | 無 |
| 啟動或測試指令 | `dotnet build DoSelect.slnx`；`dotnet ef migrations has-pending-model-changes --project src/backend/DoSelect.Infrastructure --startup-project src/backend/DoSelect.Api`（目前如實回報 pending，待 alex 排出 drop migration 後才會轉為乾淨） |
| 本機設定注意事項 | 無新增 |

## 測試與驗證

| 驗證項目 | 方法／指令 | 結果 |
|---|---|---|
| 建置 | `dotnet build DoSelect.slnx` | 通過（0 錯誤、0 警告） |
| OpenAPI／型別產物 | 本機啟動後端擷取 `/openapi/v1.json`，重新產生 `contracts/openapi.v1.json`／`schema.d.ts` | 通過，差異僅為被移除的兩個端點 |
| 單元／整合測試 | 尚未執行完整 SQL Server 測試套件（下一步進行） | 待驗證 |
| 前端檢查 | 不適用 | 不適用 |
| 人工流程 | 尚未執行 `dotnet format --verify-no-changes` | 待驗證 |

## 相容性、風險與注意事項

- 相容性影響：`SkuCompatibilityAttributes`／`SkuStorageInterfacePorts` 兩張表從未在任何環境正式上線使用（PR #34 尚未合併），無既有資料需要遷移。
- 已知風險：在 alex 排出 drop migration 前，`dotnet ef migrations has-pending-model-changes` 會持續回報 pending changes；若 CI 有把這項檢查當作合併門檻，PR #34 在這支 Migration 處理完之前無法通過該項檢查。
- 回復方式：若需要暫時恢復平行模型（不建議），可 `git revert` 本次改動；正式清理仍須透過 alex 排出的新 Migration。
- 需要其他負責人配合：**需要 alex 依 Migration Gate 排出「drop `SkuCompatibilityAttributes`／`SkuStorageInterfacePorts`」的新 Migration，並覆核 `20260826060901_AddSkuStorageInterfacePorts` 是否可直接以此收尾**。

## 未完成與後續工作

- [ ] 請 alex 排出並覆核「drop `SkuCompatibilityAttributes`／`SkuStorageInterfacePorts`」的新 Migration（Migration Gate）。
- [ ] alex 覆核通過、Migration 排定後，重新確認 `dotnet ef migrations has-pending-model-changes` 轉為乾淨。
- [ ] 執行完整 SQL Server 測試套件與 `dotnet format --verify-no-changes`，完成後於 PR #34 留言回報。

## 相關連結

- Issue：無
- Pull Request：[PR #34](https://github.com/FullStackDeveloperSecond/FinalProject/pull/34)
- Commit：HEAD `26c2ac5`（本次改動尚未提交）
- 規格／決策文件：[[05-規劃/03-需求與決策治理/決策/02-已寫回/DEC-BATCH-027-相容性來源多值規格與Checkout原子交易定版]]、[[05-規劃/02-分工與交接/工程包/Terry-商品庫存物流與報表工程包]]
- 相關開發日誌：[[2026-08-26--20-28]]（先前提出待覆核的同一支 Migration）
