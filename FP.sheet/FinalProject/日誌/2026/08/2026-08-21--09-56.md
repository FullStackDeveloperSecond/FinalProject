# 2026-08-21 09:56 客服附件上傳作業日誌

## 本階段完成內容

- 完成會員本人客服案件附件上傳端點：`POST /api/v1/support-tickets/{id}/attachments`。
- 限制 PNG、JPEG、PDF，單檔上限 10 MiB，每案件最多 3 個有效附件。
- 同時驗證副檔名、MIME 與檔案簽章，阻擋偽裝檔案。
- 串接 Microsoft Defender，掃描器不存在、逾時、取消、錯誤或惡意檔案一律拒絕。
- 採用暫存、SHA-256／簽章驗證、掃描、原子搬移、Serializable 資料庫寫入及失敗補償刪除流程。
- 資料庫交易內再次確認會員所有權、案件狀態及附件數量，避免併發繞過。
- 補償刪除失敗會留下不含實體路徑的結構化警告，並保留資料庫與清理兩個失敗原因。
- 缺少 multipart `file` 欄位時回傳 `400 validation_failed`，不進入 Application service。
- 長檔名截斷時保留合法副檔名，回應不洩漏 StorageKey、實體路徑、雜湊或內部 ID。

## Pipeline 執行紀錄

- Codex 完成需求分析、功能分析與安全邊界。
- Claude Code 直接實作正式程式，並依缺陷報告完成交易所有權重驗、補償可觀測性及 Defender 測試接縫修復。
- 獨立且不共享記憶的測試撰寫代理新增 Application、儲存、Defender、HTTP、SQL 與上傳後安全下載測試。
- 另一獨立代理執行測試並區分正式缺陷、測試缺陷與環境限制。
- 修正缺少檔案欄位的舊測試預期後，再由全新代理與主工作環境進行最終驗證。

## 最終驗證

- 獨立非 Host 焦點測試：37/37 通過。
- 主工作環境 HTTP／SQL／上傳後下載焦點測試：8/8 通過。
- 完整後端：447/447 通過（Domain 115、Application 76、Infrastructure 145、API 111）。
- `git diff --check`：通過。
- 未降低 SQL 加密，未修改登入、MFA、Data Protection、連線字串或其他安全設定。

## 尚待確認與後續工作

### 1. 後台客服附件上傳

- 什麼內容？是否在中央 Audit／Outbox 尚未完成前開放客服人員從後台上傳附件。
- 為什麼？這是 Handle 權限的寫入行為，依既有政策應留下完整稽核與事件紀錄；提前實作會造成安全與追蹤規格不一致。
- 建議怎麼做？目前維持會員本人上傳，等共同 Audit／Outbox 可用後再實作後台上傳。

### 2. Resolved 案件能否新增附件

- 什麼內容？案件已為 Resolved、但尚未 Closed／Cancelled 時，會員是否能直接新增附件，或必須先重新開啟案件。
- 為什麼？企劃文件未明確裁定；現有新增訊息行為允許 Resolved，附件若採不同規則會造成使用者操作不一致。
- 建議怎麼做？目前先與既有訊息規則一致，允許 Resolved；請組長補充明確產品規則後再統一調整。

### 3. Defender 逾時終止的 OS 層驗證

- 什麼內容？目前可確定驗證逾時／取消會觸發終止流程與 fail-closed，但無法在純單元測試直接觀察 Windows 的 `Kill(entireProcessTree: true)` 呼叫。
- 為什麼？實際 runner 內部建立 `System.Diagnostics.Process`；若要完全自動化驗證，需啟動真實子程序或再抽象化 Process 控制層。
- 建議怎麼做？目前列為受控的 OS 整合風險；若組長要求此項也必須自動化，下一階段新增 Process 控制介面與 Windows 專用整合測試。

## 如何確認

1. 執行 `dotnet test FP.dev/DoSelect.slnx -c Debug --no-restore -nologo -v minimal`，應顯示 Domain 115、Application 76、Infrastructure 145、API 111，合計 447/447 通過。
2. 執行 `git diff --check`，應無錯誤。
3. 執行 `git status --short`，可看到本階段程式、測試、報告與本日誌仍待 commit；不應把 `FP.dev/frontend/prototypes/` 或既有 Kafen Word 文件納入。
4. 查看 `.workflow/runs/20260820T100238Z-support-attachment-upload/06-final.md`，確認最終驗收摘要與延後項目。
5. 本階段尚未 commit、尚未 push，需等待使用者另行授權。
