---
文件類型: 開發日誌
建立時間: 2026-08-30 20:10
作者: yinyin
負責模組: M-20 模擬發票（Orders 讀取埠前置）
狀態: 待覆核
相關分支: feature/invoice-order-ports
相關 Commit/PR: 待建立
---

# 開發日誌

## 作者與負責範圍

| 欄位 | 內容 |
|---|---|
| 作者 | yinyin（以 haru 的備援身分實作 Orders 側元件） |
| 負責模組 | M-20（DES-22）的前置：跨模組讀取埠 |
| 本次工作範圍 | 三個埠的契約、Orders／Invoicing 兩側 adapter、provider-backed 測試 |
| 不在本次工作範圍 | 五支端點、writer、DI 註冊 |
| Orders domain review | haru |
| 狀態 | 待覆核 |

## 依據

alex 2026-08-29 對 Issue #65 的裁定：採用**修正版 A** —— 跨模組資料透過 Orders
Application 提供的埠取得，**不開放 Invoicing 直查 Orders**。`DEC-B1` 只維持個案例外，
不作為擴大直查的先例。他同時指定：先送一支只含 contracts、adapters 與
provider-backed tests 的前置 PR，**不要加會讓 Host 起不來的 endpoint 或 DI**。

## 拆掉一個混了兩種責任的介面

`dev` 的 `IInvoiceIssuanceReader` 同時有 `FindOrderSnapshotAsync`（Orders 的資料）與
`NextInvoiceSequenceAsync`（Invoicing 自己的資料）。**沒有任何一個模組能正當實作它** ——
這也是它到今天都還沒有實作的原因。

拆成三個：

| 埠 | 擁有者 | 職責 |
|---|---|---|
| `IOrderInvoiceIssuanceReader` | Orders | 開票快照 |
| `IOrderInvoiceReferenceReader` | Orders | 對外投影與擁有者欄位（批次） |
| `IInvoiceNumberSequence` | Invoicing | 流水號 |

拆的過程中還多找到一項：原本快照裡的 `OrderAlreadyHasInvoice` 也是 **Invoicing 自己的
資料**（`SimulatedInvoices`）。放在 Orders 的快照裡，等於要 Orders 去讀發票表 ——
把 Issue #65 要避免的越界反過來做一次。另外拆出 `IInvoiceExistenceReader`。

## 埠回的是狀態事實，不是 Trigger

`InvoiceIssuanceTrigger` 要區分 `OnlinePaymentSucceeded` 與 `CashOnDeliveryCollected`，
得看 `PaymentAttempt.Method` —— 那是 **Payments** 模組，不在 A1 裁定的 Orders 範圍內。
讓 Orders 去讀 PaymentAttempts 只是把同一個越界搬到上一層。

所以埠只回它確實知道的兩件事（`OrderIsCancelled`、`OrderIsPaid`），trigger 由
`IssueInvoiceService` 映射。目前這個映射**不影響任何結果**：`InvoiceCalculator` 對兩個
已付款 trigger 的處理完全相同，而 `InvoiceIssuanceTrigger` 在別處沒有用到（已 grep 確認）。

**這一項要請 alex 補裁定**：真的需要區分 COD 時，要補一個 Payments 側的埠，
而不是把 Orders 的例外放寬。

## 窄內部 Key 例外的落地

依裁定，埠可以回內部 `OrderId`／`OrderItemId`，但只限同一交易內寫既有 FK。
在 `OrderInvoicePortNotes.InternalKeyException` 具名記錄，並在每個帶內部 key 的
record 參數上引用它。商品列一定要帶得回 `OrderItemId`（DEC-P299 要求
`SimulatedInvoiceItems.OrderItemId`），非商品列（運費、組裝費）為 `null`。

## 批次查詢只提供批次形式

`IOrderInvoiceReferenceReader` **刻意不提供**「給一個內部 id 查一筆」的多載。
有那個多載，呼叫端很自然就會在迴圈裡用它，後台發票清單就變成 N+1。
測試用 `DbCommandInterceptor` 數實際往返次數，斷言三張訂單只打一個命令 ——
只斷言「回傳內容正確」是不夠的，逐筆查也會正確。

## 流水號取最大值，不是取筆數

作廢的發票仍然佔用號碼。用 `Count` 會在有作廢紀錄之後開始重複發號，
最後撞上 `UX_SimulatedInvoices_InvoiceNumber`。

月份用 `InvoiceNumber` 的前綴比對，不用 `IssuedAtUtc` 區間：號碼格式是
`DEMO-yyyyMM-NNNNNN`，序號的作用域就是號碼裡的那個月份，拿另一個欄位界定作用域，
只要兩者不一致就會發出重複號碼。

解析不出來的號碼**丟例外**，不當成 0 —— 當成 0 會讓下一次發出已經用掉的號碼，
而錯誤會出現在離原因很遠的地方。

## 測試從 1 小時 12 分變成 2 秒

第一版每條測試各建一個資料庫並跑 `MigrateAsync`。十五條跑了 **1 小時 12 分**，
本機 SQL Server 還被塞到有兩條逾時失敗。

改成整個類別共用一個資料庫（`IClassFixture` ＋ `EnsureCreatedAsync`），**2 秒**。

代價是測試之間不再天然隔離，所以：訂單各自用新的 `PublicId`，
而查「整個月」的流水號測試各自指定不同月份。這是要自己維持的紀律，寫在註解裡。

順帶抓到一個測試自己的 bug：`PaidAmount` 不在 `OrderCreation` 裡，要走
`ApplyPaymentProjection`。少了那一步，快照的 `OrderPaidAmount` 是 0 ——
而發票金額必須等於實付，這條斷言原本是紅的。

## 驗證

| 項目 | 結果 |
|---|---|
| 零警告建置 | 0 警告 0 錯誤 |
| Domain／Application／Infrastructure | 477／412／610 全綠 |
| 新增 provider-backed 測試 | 15 條（真實 SQL Server） |
| `dotnet format --verify-no-changes` | 通過 |
| DI 註冊 | **無** —— 依裁定不加，Host 行為完全未變 |
| OpenAPI 產物 | 未變（沒有端點改動） |

## 待處理

- COD trigger 需要 Payments 側的埠，請 alex 補裁定
- 本支合併進 `dev` 後才開始 M-20 的五支端點與 writer

---

## 2026-08-31 追記：alex review 回覆

### COD trigger 裁定：採用 A1

先不加 Payments port，也不讓 Orders 讀 `PaymentAttempts`。理由與我在 PR 寫的一致：
`InvoiceCalculator` 對兩個已付款 trigger 沒有行為差異，現在加跨模組介面沒有可驗證的效益。
未來真的要區分時再加 **Payments-owned** 的窄讀取埠，**不得把 Payments 查詢塞回 Orders adapter**。

不需要改任何程式。

### P3：流水號格式只檢查了一半

我寫的是 `int.TryParse(number[prefix.Length..])`，而註解與 PR 留言說的是
「不符合 `DEMO-yyyyMM-NNNNNN` 就直接拒絕」。**又是宣稱比實作多。**

`int.TryParse` 會接受：

| 值 | 為什麼不合法 |
|---|---|
| `00001` | 只有五位 |
| `+00001` | 帶正負號 |
| `000000` | `DemoInvoiceNumber.Format` 的下界是 1 |
| `1000000` | 超過六位，也超過 999999 上界 |
| `' 00001'` | 前導空白 |

原本的反向測試只有 `XXXXXX` 一條 —— 那條證明的是「完全不是數字會被擋」，
與「格式必須恰好六位數」是兩回事。

改成 `TryReadSequence`：長度必須是 6、每個字元必須是 **ASCII** 數字
（`char.IsDigit` 對全形數字也回 true）、值必須 ≥ 1。

補五條反向測試與一條 `999999` 的邊界正向測試。破壞式驗證：退回 `int.TryParse`，
五條反向測試全部變紅。

順帶新增 `SqlServerTheoryAttribute` —— 專案原本只有 `SqlServerFact`，
沒有帶 `InlineData` 的 SQL 測試寫法。

### 驗證（追記後）

| 項目 | 結果 |
|---|---|
| 零警告建置 | 0 警告 0 錯誤 |
| Infrastructure 測試 | 616／616 |
| `OrderInvoicePortsSqlServerTests` | 21／21（由 15 增加 6） |
| `dotnet format` | 通過 |
| 基底 | 已 rebase 至 `dev@5a8dc7b` |

### 2026-08-31 第三輪：我的測試把一個無效值釘成正式行為

`NextAsync` 最後是 `return used + 1;`。當月已用到 `999999` 時它回 `1_000_000`，
而 `DemoInvoiceNumber.Format` 的值域是 1～999999 —— 取號會「成功」，
接著在 `IssueInvoiceService` 格式化時丟 `ArgumentOutOfRangeException`。

**而我上一輪新加的 `TheSequenceAcceptsTheLastValidNumberOfTheMonth` 正是斷言它回 `1_000_000`。**

這跟 #63 的 C1 是同一類錯誤：**用測試把一個錯誤行為固定下來**。差別是那次我對齊的是
別人有偏差的實作，這次是我自己造出來的無效值 —— 而且測試名稱還叫「接受該月最後一個
合法號碼」，聽起來完全正當。

改成：`used >= 999999` 時丟「該月號碼已耗盡」；上限抽成 `MaximumSequence` 常數並註明
它必須與 `DemoInvoiceNumber.Format` 的值域一致。測試改成「已有 999999 時拒絕取號」，
另補正向邊界「已有 999998 時回 999999，且 `Format` 收得下」。

正向那條刻意連 `DemoInvoiceNumber.Format(issuedAtUtc, next)` 一起斷言 ——
只斷言回傳值是 999999，證明不了 formatter 真的接受它，而那正是這次出問題的接縫。

破壞式驗證：拿掉耗盡檢查，「拒絕取號」那條變紅。

| 項目 | 結果 |
|---|---|
| `OrderInvoicePortsSqlServerTests` | 22／22 |
| 零警告建置、`dotnet format` | 通過 |
