---
文件類型: 開發日誌
建立時間: 2026-08-20 12:38
作者: kafen／Codex
負責模組: 客服分支與 SH-05 安全基礎整合
狀態: 已完成
相關分支: feature/support-tickets
相關 Commit/PR: 本機 Rebase 與相容性 Commit；尚未 Push
---

# 開發日誌

## 摘要

將 `feature/support-tickets` rebase 至最新 `origin/dev`（`d9bd95c`），保留 dev 的雙 Cookie Scheme、MFA、正式 Policy、CORS 與全域 Antiforgery，並把客服 API 與測試改接新安全基礎。移除已被 SH-05 淘汰的 DEV 假登入與重複 Support Policy。

## Rebase 與整合結果

| 項目 | 結果 |
|---|---|
| 新基準 | `origin/dev`：`d9bd95c` |
| 客服 commit（重寫後） | `8ce5d71`、`7a9a412` |
| 衝突 | customer-web API client、API `Program.cs` |
| 衝突處理 | 合併 Antiforgery client；保留 `AddDoSelectSecurity`，只追加客服 DI |
| Push | 未執行 |

## 安全契約調整

- 會員客服 Controller 使用 `DoSelectPolicies.Member`。
- 後台 claim、SLA、工作台使用 `DoSelectPolicies.SupportTicketHandle`。
- 移除重複的 `SupportAuthorizationPolicies` 與註冊。
- 移除 `/api/v1/dev/test-sign-in`、`test-sign-out`、DEV widget 與 localStorage 假登入。
- 所有客服 POST 整合測試先取得正式 Antiforgery token，再送 `X-XSRF-TOKEN`。
- 測試專用驗證保留在 test host，並模擬正式 account-type、MFA 與角色 claims；production security 未放寬。

## 驗證

| 驗證 | 結果 |
|---|---|
| `dotnet build FP.dev/DoSelect.slnx --no-restore -warnaserror` | 通過，0 警告／0 錯誤 |
| `dotnet test FP.dev/DoSelect.slnx --no-build --no-restore` | 356/356 通過 |
| customer-web typecheck | 通過 |
| customer-web lint | 通過，0 警告 |
| customer-web test | 19/19 通過 |
| customer-web production build | 通過 |

## 後續事項

- 真實會員／管理員登入畫面與簽發 Cookie 流程仍由 Identity 主責整合；客服頁不再維護第二套假登入。
- 尚未 Push；依約等待第一優先基本功能完成。
- 前台新設計仍須先提供示意圖並取得 kafen 確認。
