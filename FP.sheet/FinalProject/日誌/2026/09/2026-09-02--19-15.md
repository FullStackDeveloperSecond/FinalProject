---
作者: alex
日期: 2026-09-02 19:15
工作包:
  - WP-A05
  - AI-09
---

# WP-A05／AI-09 Live Runner 與完整成本保存

## 更改內容

- 依組員與組長確認，將 120 筆繁中評估案例由 `draft` 提升為 `approved`。
- 新增 `DoSelect.AiEvals` 本機手動工具並掛入 `DoSelect.slnx`；預設只做 Dry Run，只有明確 `--execute` 與正值 `--stop-after-cost-usd` 才可呼叫 OpenAI。
- Runner 支援 Split、Trial、最大案例數與指定案例，重用正式 `OpenAiProductSearchClient`、`OpenAiResponsesClient`、Prompt Envelope、strict Schema 與合成／去識別 Fixture。
- 結果預設寫入 `.run/ai-evals/<UTC timestamp>`，包含 `case-results.jsonl`、`summary.json` 與 `human-review.md`；不保存 API Key、Authorization Header 或真實個資。
- 修正已完成 Responses 在要求轉人工或 strict 輸出不合法重試時遺失 Token 的問題；同一互動聚合所有具 usage 的完成嘗試，降級 Interaction 也保存該 usage 與既有成本公式。
- 新增隱藏輸入的 `configure-openai-eval-secrets.ps1`，只把 API Key 與 2026-09-02 核對的 Luna／Terra單價寫入 .NET User Secrets，不啟用網站 AI、不發出請求。
- 寫回 DEC-BATCH-047、AI 測試規格、追蹤表與 Alex 個人交付計畫。

## 定義內容

- `DEC-P357`：Terry／Kafen 主標與 Alex 第二審已完成；案例變更仍須升版並重新覆核。
- `DEC-P358`：任何已完成且帶 usage 的 Responses 嘗試都必須計費；模型回答、轉人工與結構修復重試採同一原則。
- Release Dry Run 固定 36 筆，其中 33 筆可執行 Live、3 筆只屬 deterministic-only；三輪共規劃 129 次模型請求。
- 成本停止線是在開始下一筆前檢查；已送出的單筆請求可能讓最終值略高於指定上限，因此第一次只允許兩案例單輪、US$0.10。
- 完整 Release baseline 必須等煙霧測試取得實際 Token／成本後另行核准，不因 Runner 可執行就自動發送 129 次請求。

## 取用方式

- Dry Run：`dotnet run --project .\tools\DoSelect.AiEvals\DoSelect.AiEvals.csproj -- --project-root . --split release --trials 3`
- 本機設定：`.\scripts\configure-openai-eval-secrets.ps1`
- 煙霧測試：依 `evals/ai/v1/README.md` 指令執行 `SEARCH-NOVICE-001` 與 `SUPPORT-POLICY-001`。
- 正式結果不得從 `.run` 直接宣稱完成；須完成 deterministic 門檻與 `human-review.md` 人工覆核後，再保存核准報告。

## 驗證紀錄

- 資料產生／check／validator：120 筆通過，隱私掃描未發現真實 Email、台灣手機、API Secret 或 Private Key。
- Runner／Responses／Product Search focused tests：22／22 通過。
- AI Support Orchestrator focused tests：12／12 通過。
- `dotnet format DoSelect.slnx --verify-no-changes --no-restore`：通過。
- `dotnet build DoSelect.slnx --no-restore --configuration Debug`：0 錯誤；僅本機無法連到 NuGet 弱點資料來源的既知 `NU1900` 警告。
- 全方案測試嘗試中，Domain 482／482、Application 577／577 通過；其餘 SQL Server Provider／API Integration 案例因本機 SQL Server 回報「執行個體要求加密，但此電腦不支援」而無法形成有效結果，屬測試環境阻擋，未誤列為本次 AI 變更失敗或完整方案綠燈。
- Mock Responses Runner 驗證 usage、成本與三份結果產物寫入後清理：通過。
- Release Dry Run：36／33／3、三輪 129 次，Annotations Approved、Live Ready。
- 缺少 User Secrets 時的 `--execute` 預檢：在任何外部呼叫前列出 API Key 與四個單價缺口並停止。
- 真實 OpenAI：尚未呼叫；目前沒有付費結果、P95 或正式品質 verdict。
