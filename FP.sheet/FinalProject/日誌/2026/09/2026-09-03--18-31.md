---
作者: alex
日期: 2026-09-03 18:31
工作包:
  - WP-A05
  - AI-09
---

# AI-09 初次煙霧測試與修正版

## 更改內容

- 確認 OpenAI API Key 與四個模型單價已存在 User Secrets；未讀回或輸出 Secret 值。
- 依組長授權執行 `SEARCH-NOVICE-001` 與 `SUPPORT-POLICY-001` 單輪 Live 煙霧測試，停止線 US$0.10；實際成本 US$0.001880。
- 商品搜尋為 0 Token／0 成本的 `Unavailable`；對照官方 Structured Outputs 支援子集合與送出 Schema，移除兩處 `uniqueItems`，改在 Adapter 映射後拒絕重複品牌。
- 客服案例通過 Schema 與引用，但人工必答點「個案依訂單政策版本」缺少 Fixture 依據；保留正式期待並補入訂單成立時退貨政策版本快照。
- 資料集提升為 `zh-TW-v1.0.1-draft`、Fixture 提升為 `v1.0.1`；105 筆未受影響案例維持 `approved`，15 筆 `SUPPORT-POLICY` 改回 `draft`。
- 寫回 DEC-BATCH-048、AI 評估規格、追蹤表、README 與 Alex 個人交付計畫。
- 依組長後續授權補強 Live Runner：四個 Token 單價須大於 0；指定的任一 `case-id` 不存在於所選 split 時立即拒絕，不再靜默少測。
- 覆核前確認 15 筆政策案例需要的具體金額、期限與例外未存在於舊 Fixture 內容；依組長授權補齊兩個核准政策快照，資料集升為 `zh-TW-v1.0.2-draft`、Fixture 升為 `v1.0.2`，並建立 Kafen／Alex 逐案覆核表。
- 修正 Live Runner 的政策引用版本：不再固定回報 `v1.0.0`，改讀 Fixture 檔案實際版本。

## 定義內容

- Responses strict Schema 只使用官方支援的 JSON Schema 子集合；`uniqueItems` 的商業限制由後端 deterministic 驗證承擔。
- Fixture 改動不得沿用舊核准狀態；只有受影響的政策案例需由 Kafen 主標、Alex 第二審。
- 初次結果的 Commit SHA 指向尚未包含 Runner 的基準，且修正版尚未覆核，因此只屬診斷，不是 baseline。
- 本次修正授權不包含第二次 Live 呼叫；重跑前須完成覆核、測試、commit 與新的費用授權。
- Live 成本設定與案例選取屬模型呼叫前強制 Gate；不得以預設零單價或部分命中的案例集合繼續執行。

## 驗證紀錄

- 修正前 `OpenAiProductSearchClientTests`：8 項中 2 項如預期失敗，分別證明 Schema 含 `uniqueItems`、重複品牌會被接受。
- 修正後同一聚焦範圍：8／8 通過。
- 修正版聚焦回歸：Infrastructure 23／23、Application 12／12 通過。
- Runner 防呆回歸加入後，Infrastructure 聚焦範圍 26／26 通過。
- 完整政策 Fixture 與實際引用版本回歸：`LiveEvaluationPlanTests` 9／9 通過；`zh-TW-v1.0.2-draft` 產生與 validator 120／120 通過。
- 修正版完整聚焦回歸：Infrastructure 28／28、Application 12／12 通過；雙案例 dry-run 仍以 `ANNOTATIONS_NOT_APPROVED` 阻擋且未呼叫 OpenAI。
- 組長確認 Kafen 已完成 15 筆 `SUPPORT-POLICY` 主標；Alex 第二審補上第 15 案 AI 禁止寫入的正式驗收來源後，15 筆均改為 `approved`。Release 36 案例／3 trials 與雙案例 dry-run 均為 `AnnotationsApproved=true`、`IsLiveReady=true`，未呼叫 OpenAI。
- `zh-TW-v1.0.1-draft` 產生、產物同步與 validator：120 筆通過；隱私掃描未發現真實 Email、台灣手機、API Secret 或 Private Key。
- Release 36 案例／3 trials 與雙案例煙霧測試的 dry-run 均以 `ANNOTATIONS_NOT_APPROVED` 正確阻擋，未發出模型請求。
- `dotnet build DoSelect.slnx --no-restore`：0 error；僅 NuGet 弱點來源暫時不可達的 `NU1900` warning。`dotnet format --verify-no-changes` 與 `git diff --check` 通過。
- 本機未安裝 Gitleaks；變更檔的保守規則掃描僅命中 3 個測試用合成金鑰。完整 Git 歷史 Secret Scan 仍交由 PR CI 執行。
- 第二次 Live：未執行，新增成本為 US$0。
