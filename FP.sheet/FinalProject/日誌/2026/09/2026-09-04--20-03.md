---
作者: alex
日期: 2026-09-04 20:03
工作包:
  - WP-A05
  - AI-09
---

# AI v1.0.3 覆核與 deterministic-only 證據

## 更改內容

- 記錄 `SEARCH-CREATOR-008`、`SEARCH-CREATOR-013` 的 Terry 主標與 Alex 第二審均通過，將兩案標註改為 `approved`。
- 重建並驗證 120 筆資料集；Release split 為 36 筆，其中 22 筆 live、14 筆 deterministic-only，三輪規劃 66 次模型請求。
- 新增 14 筆 deterministic-only orchestration 測試，直接重用正式 `AiProductSearchOrchestrator`、`CompatibilityEvaluator` 與 `AiSupportRequestGate`。
- 新增商品搜尋無結果 UI 測試，固定無結果提示與放寬條件建議。
- 建立 T2 原始測試產物與可提交的摘要報告，並同步更新 AI 評估說明、正式規格、追蹤表、M 功能矩陣與 Alex 工程包。
- 完成 Commit `f195c453` 既有 6 案 Smoke 的正式人工內容複核：6／6 已審、3 Pass／3 Fail；保留歷史 Run 的 `FAIL`。
- 以零成本 Dry Run 驗證後續 v4 Smoke：沿用 6 個回歸案例、各 1 輪，共規劃 6 次模型請求，標註與執行前置均就緒。

## 定義內容

- `zh-TW-v1.0.3-draft` 的 120 筆標註目前均為 `approved`；Dry Run 必須回報 `AnnotationsApproved=true`、`IsLiveReady=true`。
- 14 筆 deterministic-only 案例不得呼叫外部模型；正式 Application／Domain 行為必須 14／14 通過且 0 略過。
- 本次後端 14／14、無結果 UI 7／7 通過，外部模型呼叫 0、Token 0、成本 US$0。
- 原始 TRX／JSON 保存在 Git 忽略的 `.run/ai-evals/20260904T120035Z-deterministic/`；儲存庫只保存去敏摘要報告。
- 目前結果在未提交工作樹上執行，以基準 Commit＋輸入 SHA-256 識別；正式合併前須在確定 Commit 重跑，形成 revision-pinned 證據。
- 這些綠燈不代表 `product-search-v4` 的 Live 品質、P95、Token 或成本通過。下一步是整理 v4 付費 Smoke 的案例、輪次與成本停止線；任何新付費 Smoke／baseline 仍須另行授權。
